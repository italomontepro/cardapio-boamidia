---
phase: 04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/db/schema.ts
  - src/db/migrations/0003_units_lat_lng.sql
  - scripts/verify-units-location.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The units table has nullable lat and lng numeric(10,7) columns"
    - "Existing Phase-3 units remain valid with lat/lng = null after migration"
    - "leaflet, react-leaflet, and @types/leaflet are installed at the versions research verified"
    - "An automated verify script proves migration correctness and runs in ~10s"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "units table with lat/lng numeric(10,7) nullable columns + mode:'number'"
      contains: "lat: numeric"
    - path: "src/db/migrations/0003_units_lat_lng.sql"
      provides: "Additive ALTER TABLE adding lat/lng to units"
      contains: "ALTER TABLE"
    - path: "scripts/verify-units-location.ts"
      provides: "Wave 0 integration verification (migration + actions + geocoding assertions)"
      contains: "ALL CHECKS PASSED"
  key_links:
    - from: "src/db/schema.ts"
      to: "src/db/migrations/0003_units_lat_lng.sql"
      via: "drizzle-kit generate"
      pattern: "lat.*numeric"
---

<objective>
Lay the data + tooling foundation for unit geolocation: install the map/geocoding npm packages, add nullable `lat`/`lng` columns to the `units` table via an additive Drizzle migration, and create the Wave 0 verification script that all subsequent tasks in this phase sample against.

Purpose: Phase 5's "nearest unit" feature needs lat/lng on units. This plan makes the column exist (nullable, so existing units stay valid per D-06) and gives executors a fast automated check (`npx tsx scripts/verify-units-location.ts`) to run after every schema/actions/geocoding change.
Output: Updated `units` schema, migration 0003 applied to live Supabase, installed packages, and `scripts/verify-units-location.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-CONTEXT.md
@.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md
@.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-VALIDATION.md

<interfaces>
<!-- Current units table (src/db/schema.ts) — extend this exact definition, do not rewrite the file -->
export const units = pgTable('units', {
  id: uuid('id').primaryKey().defaultRandom(),
  restaurantId: uuid('restaurant_id').notNull().references(() => restaurants.id, { onDelete: 'cascade' }),
  name: text('name').notNull(),
  slug: text('slug').notNull(),
  address: text('address'),
  whatsappNumber: text('whatsapp_number'),
  hours: text('hours'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
}, (table) => [
  unique('units_restaurant_slug_unique').on(table.restaurantId, table.slug),
  index('idx_units_restaurant_id').on(table.restaurantId),
])

<!-- `numeric` is ALREADY imported in src/db/schema.ts (used by products.price). No new import needed. -->
<!-- Drizzle/Supabase connection convention (drizzle.config.ts + CLAUDE.md):
     drizzle-kit generate/migrate uses DATABASE_URL (Session Mode, port 5432);
     app runtime + verify scripts use DATABASE_URL_RUNTIME (Transaction Mode, port 6543, prepare:false). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install leaflet/react-leaflet packages and add lat/lng columns to units schema</name>
  <files>package.json, src/db/schema.ts, src/db/migrations/0003_units_lat_lng.sql</files>
  <read_first>
    - src/db/schema.ts (current units table; numeric is already imported on line 8)
    - package.json (current deps; confirm leaflet/react-leaflet absent)
    - drizzle.config.ts (migration output dir = ./src/db/migrations, dialect postgresql, url = DATABASE_URL)
    - .planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md ("Standard Stack" + "Drizzle migration" code example + Pitfall 4)
  </read_first>
  <action>
    1. Install exact-verified packages (versions confirmed against npm registry on 2026-06-17):
       `npm install leaflet@1.9.4 react-leaflet@5.0.0`
       `npm install -D @types/leaflet@1.9.21`
       (react-leaflet 5.0.0 peerDeps are leaflet ^1.9.0 / react ^19.0.0 / react-dom ^19.0.0 — exact match with this project's react 19.2.4.)

    2. In `src/db/schema.ts`, add TWO new columns to the existing `units` pgTable definition, immediately after the `hours: text('hours'),` line and before `createdAt`:
       ```
       lat: numeric('lat', { precision: 10, scale: 7, mode: 'number' }),
       lng: numeric('lng', { precision: 10, scale: 7, mode: 'number' }),
       ```
       Both are nullable (no `.notNull()`) per D-06/D-07. `mode: 'number'` is MANDATORY (Pitfall 4) — without it Drizzle infers `lat: string | null`, breaking Phase 5 distance math. Do NOT change any other line in schema.ts (keep `numeric` import as-is, keep the unitsRelations block as-is). Do NOT touch the unique/index constraint array.

    3. Generate the migration:
       `npx drizzle-kit generate --name units_lat_lng`
       This produces `src/db/migrations/0003_units_lat_lng.sql` containing additive `ALTER TABLE "units" ADD COLUMN "lat" numeric(10, 7);` and `ADD COLUMN "lng" numeric(10, 7);`. If drizzle-kit names the file differently (e.g. a random suffix), rename the generated `.sql` and its meta journal entry to `0003_units_lat_lng.sql` for consistency, OR accept the generated name and note the actual filename in the SUMMARY (do NOT hand-write the SQL if generate succeeds).

    4. Apply the migration to the live Supabase DB (Session Mode / DATABASE_URL per drizzle.config.ts):
       `npx drizzle-kit migrate`
       If `migrate` is not the project's apply mechanism, fall back to `npx drizzle-kit push` ONLY if prior migrations were applied that way — check `src/db/migrations/meta/` journal and STATE.md history first. Prefer `migrate` since 0000-0002 are journaled migrations.
  </action>
  <verify>
    <automated>grep -q "lat: numeric('lat', { precision: 10, scale: 7, mode: 'number' })" src/db/schema.ts && grep -q "lng: numeric('lng', { precision: 10, scale: 7, mode: 'number' })" src/db/schema.ts && ls src/db/migrations/0003_*.sql && grep -qi "ADD COLUMN" src/db/migrations/0003_*.sql && node -e "const p=require('./package.json'); if(!p.dependencies.leaflet||!p.dependencies['react-leaflet']||!p.devDependencies['@types/leaflet']) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "lat: numeric('lat', { precision: 10, scale: 7, mode: 'number' })" src/db/schema.ts` matches
    - `grep "lng: numeric('lng', { precision: 10, scale: 7, mode: 'number' })" src/db/schema.ts` matches
    - `package.json` dependencies include `leaflet` (1.9.4) and `react-leaflet` (5.0.0); devDependencies include `@types/leaflet`
    - A file matching `src/db/migrations/0003_*.sql` exists and contains `ADD COLUMN` for both `lat` and `lng`
    - `npx tsc --noEmit` (or `npm run lint`) reports no new type errors from schema.ts
  </acceptance_criteria>
  <done>units schema has nullable lat/lng numeric(10,7) number-mode columns, migration 0003 generated and applied to live Supabase, leaflet/react-leaflet/@types/leaflet installed.</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 verification script scripts/verify-units-location.ts</name>
  <files>scripts/verify-units-location.ts, package.json</files>
  <read_first>
    - scripts/verify-availability.ts (EXACT pattern to replicate: dotenv walk-up env loader, dynamic imports after config(), fixture create/assert/cleanup, final "ALL CHECKS PASSED" + process.exit)
    - src/db/schema.ts (units table now has lat/lng after Task 1 — assert against these)
    - package.json scripts block (mirror the verify-availability registration style)
    - .planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-VALIDATION.md ("Wave 0 Requirements" + "Per-Task Verification Map")
  </read_first>
  <action>
    Create `scripts/verify-units-location.ts` copying the EXACT structure of `scripts/verify-availability.ts`:
    - Same dotenv walk-up loader block (lines 12-25 of verify-availability.ts) verbatim.
    - In `main()`, do dynamic imports AFTER config(): `const { db } = await import('../src/db')`, `const { restaurants, units } = await import('../src/db/schema')`, `const { eq, and } = await import('drizzle-orm')`.
    - Pick a seeded restaurant: `const [seedRestaurant] = await db.select().from(restaurants).limit(1)`; throw `'verify-units-location: no restaurant seeded — run npm run seed first'` if absent. Capture `restaurantId`.
    - `const runId = Date.now()`.

    Assertions (each logs `... PASS` on success, throws with a descriptive message on failure):

    1. UNIT-NULL-LOCATION-OK: Insert a unit with NO lat/lng:
       `const [u1] = await db.insert(units).values({ restaurantId, name: 'ZZ Loc NoGeo '+runId, slug: 'zz-loc-nogeo-'+runId, whatsappNumber: '+5511999990000' }).returning()`
       Assert `u1.lat === null && u1.lng === null`. Else throw `LOCATION-NULL FAIL: expected null lat/lng, got ${u1.lat}/${u1.lng}`. Log `LOCATION-NULL PASS`.

    2. UNIT-WITH-LOCATION-PERSIST: Insert a unit WITH lat/lng:
       `const [u2] = await db.insert(units).values({ restaurantId, name: 'ZZ Loc Geo '+runId, slug: 'zz-loc-geo-'+runId, whatsappNumber: '+5511999990000', lat: -23.5505, lng: -46.6333 }).returning()`
       Assert `typeof u2.lat === 'number' && Math.abs(u2.lat - (-23.5505)) < 0.0001 && Math.abs(u2.lng - (-46.6333)) < 0.0001`. This proves mode:'number' (NOT string). Else throw `LOCATION-PERSIST FAIL: expected number -23.5505/-46.6333, got ${typeof u2.lat} ${u2.lat}/${u2.lng}`. Log `LOCATION-PERSIST PASS`.

    3. GEOCODE-SUCCESS (live network call to Nominatim, gated so a network failure doesn't false-fail the schema checks): dynamically import the geocode helper IF it exists yet (it is created in Plan 02). Wrap in try/catch around the import:
       ```
       try {
         const { geocodeAddress } = await import('../src/lib/units/geocode')
         const ok = await geocodeAddress('Avenida Paulista, Sao Paulo, Brazil')
         if (!ok || typeof ok.lat !== 'number' || typeof ok.lng !== 'number') throw new Error(`GEOCODE-SUCCESS FAIL: expected {lat,lng}, got ${JSON.stringify(ok)}`)
         console.log('GEOCODE-SUCCESS PASS')
         const bad = await geocodeAddress('zzzqxqxqx not a real place 999888')
         if (bad !== null) throw new Error(`GEOCODE-NOTFOUND FAIL: expected null, got ${JSON.stringify(bad)}`)
         console.log('GEOCODE-NOTFOUND PASS')
       } catch (e) {
         if (e instanceof Error && e.message.includes('Cannot find module')) {
           console.log('GEOCODE SKIP (geocode.ts not created yet — Plan 02)')
         } else { throw e }
       }
       ```
       NOTE: respect Nominatim's 1 req/sec limit — the two calls above are sequential and few; do NOT add loops. Use a 1100ms `await new Promise(r=>setTimeout(r,1100))` between the two geocode calls to stay policy-safe.

    - CLEANUP: `await db.delete(units).where(and(eq(units.id, u1.id), eq(units.restaurantId, restaurantId)))` and same for `u2.id`.
    - End with `console.log('ALL CHECKS PASSED')` then `main().then(() => process.exit(0)).catch((e) => { console.error('FAIL:', e.message); process.exit(1) })`.

    Add to package.json scripts: `"verify-units-location": "tsx scripts/verify-units-location.ts"`.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-units-location.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-units-location.ts` exists and contains the strings `LOCATION-NULL PASS`, `LOCATION-PERSIST PASS`, and `ALL CHECKS PASSED`
    - `grep "verify-units-location" package.json` matches (script registered)
    - Running `npx tsx scripts/verify-units-location.ts` exits 0 and prints `ALL CHECKS PASSED` (GEOCODE block prints `GEOCODE SKIP` at this point since geocode.ts is created in Plan 02 — that is expected and acceptable)
    - The script uses the dotenv walk-up loader and dynamic-import-after-config pattern (grep for `config({ path: envFile })` and `await import('../src/db')`)
  </acceptance_criteria>
  <done>verify-units-location.ts runs green against live Supabase, proving lat/lng nullable + number-mode persistence; geocode assertions are wired but skip-tolerant until Plan 02 adds geocode.ts.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-units-location.ts` exits 0 with `ALL CHECKS PASSED`.
- `src/db/migrations/0003_*.sql` exists and was applied (live `units` table has lat/lng columns).
- `npm run lint` / `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- units table accepts nullable lat/lng (existing rows unaffected, new rows can omit or set them).
- lat/lng round-trip as JS numbers (not strings) via Drizzle mode:'number'.
- leaflet@1.9.4 + react-leaflet@5.0.0 + @types/leaflet@1.9.21 installed.
- Wave 0 verify script green and registered as `npm run verify-units-location`.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-01-SUMMARY.md` (record the actual migration filename if drizzle-kit chose a different suffix).
</output>
