---
phase: 04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/lib/units/schema.ts
  - src/lib/units/geocode.ts
  - src/lib/units/actions.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "upsertUnitSchema accepts optional/nullable lat and lng (Step 3 is skippable per D-07)"
    - "createUnit and updateUnit persist provided lat/lng and store null when omitted"
    - "A geocodeUnitAddress Server Action returns {lat,lng,displayName} for a valid address and {error} for not-found"
    - "Nominatim is called only server-side with a descriptive User-Agent header"
  artifacts:
    - path: "src/lib/units/schema.ts"
      provides: "upsertUnitSchema with optional nullable lat/lng + UpsertUnitInput type"
      contains: "lat: z.number"
    - path: "src/lib/units/geocode.ts"
      provides: "server-only Nominatim fetch wrapper geocodeAddress()"
      contains: "User-Agent"
    - path: "src/lib/units/actions.ts"
      provides: "createUnit/updateUnit persisting lat/lng + geocodeUnitAddress Server Action"
      contains: "geocodeUnitAddress"
  key_links:
    - from: "src/lib/units/actions.ts"
      to: "src/lib/units/geocode.ts"
      via: "import geocodeAddress"
      pattern: "from './geocode'"
    - from: "src/lib/units/actions.ts"
      to: "units.lat / units.lng"
      via: "db.insert/update .values({ lat, lng })"
      pattern: "lat:.*parsed.data.lat"
---

<objective>
Wire lat/lng through the server layer: extend `upsertUnitSchema` with optional nullable lat/lng, persist them in `createUnit`/`updateUnit`, and add a server-only Nominatim geocoding helper plus a `geocodeUnitAddress` Server Action (D-04/D-05) that the Step 3 map UI will call.

Purpose: The wizard (Plan 03) needs (a) a schema that allows omitting location, (b) actions that save coordinates, and (c) a callable Server Action to turn an address string into lat/lng. Geocoding MUST be server-side — browsers can't set the custom User-Agent Nominatim requires (Pitfall 3).
Output: Updated schema.ts, new geocode.ts, updated actions.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-CONTEXT.md
@.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md

<interfaces>
<!-- CURRENT upsertUnitSchema (src/lib/units/schema.ts) — extend, do not rewrite the whatsapp logic -->
export const upsertUnitSchema = z.object({
  id: z.string().uuid().optional(),
  name: z.string().trim().min(1, 'Nome é obrigatório.').max(120, ...),
  address: z.string().trim().max(255).optional().or(z.literal('')),
  hours: z.string().trim().max(255).optional().or(z.literal('')),
  whatsappNumber: z.string().trim().min(1, ...).refine(isValidPhoneNumber...).transform(parsePhoneNumber...),
})
export type UpsertUnitInput = z.infer<typeof upsertUnitSchema>

<!-- CURRENT createUnit/updateUnit signatures (src/lib/units/actions.ts) — '_server' file -->
createUnit(input: { name; address?; whatsappNumber; hours? }): Promise<{success:true}|{error:Record<string,string[]>}>
updateUnit(input: { id?; name; address?; whatsappNumber; hours? }): Promise<...same...>
// Both: getCurrentAdmin() guard -> upsertUnitSchema.safeParse(input) -> db.insert/update(units).values({...}) -> revalidatePath('/painel/unidades')

<!-- Nominatim contract (research-verified):
GET https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&q=<encoded>
Headers: { 'User-Agent': 'CardapioBoaMidia/1.0 (contato@suporte.folkscomp.com)' }  // MANDATORY, stock UA rejected
Response: Array<{ lat: string; lon: string; display_name: string }> | []  // note 'lon' not 'lng' -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional lat/lng to upsertUnitSchema</name>
  <files>src/lib/units/schema.ts</files>
  <read_first>
    - src/lib/units/schema.ts (current schema; keep whatsappNumber refine/transform untouched)
    - .planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md ("Extending upsertUnitSchema" code example)
  </read_first>
  <behavior>
    - upsertUnitSchema.safeParse({name, whatsappNumber}) succeeds with lat/lng undefined (Step 3 optional, D-07)
    - upsertUnitSchema.safeParse({name, whatsappNumber, lat: -23.5, lng: -46.6}) succeeds; data.lat === -23.5
    - upsertUnitSchema.safeParse({..., lat: 200}) fails (lat out of -90..90 range)
    - upsertUnitSchema.safeParse({..., lat: null, lng: null}) succeeds (nullable)
  </behavior>
  <action>
    In `src/lib/units/schema.ts`, add two fields to the existing `upsertUnitSchema` object, after the `whatsappNumber` field (keep ALL existing fields and the whatsapp refine/transform exactly as-is):
    ```
    lat: z.number().min(-90, 'Latitude inválida.').max(90, 'Latitude inválida.').optional().nullable(),
    lng: z.number().min(-180, 'Longitude inválida.').max(180, 'Longitude inválida.').optional().nullable(),
    ```
    Keep the `export type UpsertUnitInput = z.infer<typeof upsertUnitSchema>` line. Do not add separate per-step schemas — the wizard (Plan 03) will gate steps with `form.trigger([...fields])` against this single schema (RESEARCH Pattern 3).
  </action>
  <verify>
    <automated>node -e "require('tsx/cjs'); const {upsertUnitSchema}=require('./src/lib/units/schema.ts'); const a=upsertUnitSchema.safeParse({name:'X',whatsappNumber:'(11) 99999-9999'}); const b=upsertUnitSchema.safeParse({name:'X',whatsappNumber:'(11) 99999-9999',lat:-23.5,lng:-46.6}); const c=upsertUnitSchema.safeParse({name:'X',whatsappNumber:'(11) 99999-9999',lat:200,lng:0}); if(!a.success){console.error('no-geo should pass');process.exit(1)} if(!b.success||b.data.lat!==-23.5){console.error('geo should pass with number');process.exit(1)} if(c.success){console.error('lat 200 should fail');process.exit(1)} console.log('SCHEMA OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "lat: z.number" src/lib/units/schema.ts` matches
    - `grep "lng: z.number" src/lib/units/schema.ts` matches and includes `.optional().nullable()`
    - The verify node command prints `SCHEMA OK` and exits 0
    - Existing whatsappNumber refine/transform lines are unchanged (grep `isValidPhoneNumber` still present)
  </acceptance_criteria>
  <done>upsertUnitSchema accepts optional/nullable lat (-90..90) and lng (-180..180); omission and null both pass; out-of-range fails.</done>
</task>

<task type="auto">
  <name>Task 2: Create geocode.ts Nominatim wrapper and persist lat/lng in createUnit/updateUnit + add geocodeUnitAddress Server Action</name>
  <files>src/lib/units/geocode.ts, src/lib/units/actions.ts</files>
  <read_first>
    - src/lib/units/actions.ts (current createUnit/updateUnit — replicate the getCurrentAdmin guard + safeParse + try/catch + revalidatePath pattern exactly)
    - src/lib/units/schema.ts (now has lat/lng after Task 1)
    - .planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md ("Pattern 2: Server-side Nominatim geocoding via Server Action" code example + Pitfall 3)
  </read_first>
  <action>
    PART A — Create `src/lib/units/geocode.ts` (server-only module, NO 'use server' directive — it is a plain helper imported by the action):
    ```
    const NOMINATIM_URL = 'https://nominatim.openstreetmap.org/search'

    export interface GeocodeResult { lat: number; lng: number; displayName: string }

    export async function geocodeAddress(query: string): Promise<GeocodeResult | null> {
      const url = `${NOMINATIM_URL}?format=jsonv2&limit=1&q=${encodeURIComponent(query)}`
      const res = await fetch(url, {
        headers: { 'User-Agent': 'CardapioBoaMidia/1.0 (contato@suporte.folkscomp.com)' },
      })
      if (!res.ok) return null
      const data = (await res.json()) as Array<{ lat: string; lon: string; display_name: string }>
      if (!Array.isArray(data) || data.length === 0) return null
      return { lat: parseFloat(data[0].lat), lng: parseFloat(data[0].lon), displayName: data[0].display_name }
    }
    ```
    NOTE the response field is `lon` (Nominatim) which we map to `lng`. The User-Agent string is MANDATORY (Pitfall 3) — do not omit it. Do NOT call this from client code.

    PART B — In `src/lib/units/actions.ts`:
    1. Add import at top: `import { geocodeAddress } from './geocode'`.
    2. Widen `createUnit` input type to include `lat?: number | null; lng?: number | null` and `updateUnit` input type likewise. Add `lat` and `lng` to the `db.insert(units).values({...})` call:
       `lat: parsed.data.lat ?? null,`
       `lng: parsed.data.lng ?? null,`
       (place after `hours: parsed.data.hours || null,`). Do the same in `updateUnit`'s `db.update(units).set({...})` call.
    3. Append a new Server Action at the end of the file (after deleteUnit). Because actions.ts is already `'use server'`, this export is automatically a Server Action:
       ```
       export async function geocodeUnitAddress(
         address: string
       ): Promise<{ success: true; lat: number; lng: number; displayName: string } | { error: string }> {
         const admin = await getCurrentAdmin()
         if (!admin?.restaurantId) return { error: 'Não autorizado.' }
         const q = address.trim()
         if (!q) return { error: 'Endereço vazio.' }
         const result = await geocodeAddress(q)
         if (!result) return { error: 'Endereço não encontrado. Ajuste o pino manualmente no mapa.' }
         return { success: true, lat: result.lat, lng: result.lng, displayName: result.displayName }
       }
       ```
       Keep the getCurrentAdmin guard for parity with the other actions (server-side authorization, never trust client). Do NOT add a debounce or loop — the UI (Plan 03) triggers this only on explicit "Buscar" click + once on Step 3 entry (D-04/D-05), staying within the 1 req/sec policy.
  </action>
  <verify>
    <automated>npx tsx scripts/verify-units-location.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/units/geocode.ts` exists, exports `geocodeAddress`, and contains the literal `'User-Agent': 'CardapioBoaMidia/1.0 (contato@suporte.folkscomp.com)'` and maps `lon` -> `lng`
    - `grep "geocodeUnitAddress" src/lib/units/actions.ts` matches; the action has a `getCurrentAdmin()` guard
    - `grep "from './geocode'" src/lib/units/actions.ts` matches
    - `createUnit` and `updateUnit` both include `lat:` and `lng:` in their values/set objects (grep `parsed.data.lat`)
    - `npx tsx scripts/verify-units-location.ts` now prints `GEOCODE-SUCCESS PASS` and `GEOCODE-NOTFOUND PASS` (no longer SKIP) AND `ALL CHECKS PASSED`, exit 0
  </acceptance_criteria>
  <done>geocode.ts wraps Nominatim server-side with the required User-Agent; createUnit/updateUnit persist lat/lng (null when omitted); geocodeUnitAddress Server Action returns coordinates for valid addresses and a friendly error otherwise; verify script fully green including live geocode assertions.</done>
</task>

</tasks>

<verification>
- `npx tsx scripts/verify-units-location.ts` exits 0 with `LOCATION-NULL PASS`, `LOCATION-PERSIST PASS`, `GEOCODE-SUCCESS PASS`, `GEOCODE-NOTFOUND PASS`, `ALL CHECKS PASSED`.
- `npm run lint` / `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Schema allows omitting location (D-07) and validates coordinate ranges.
- Actions persist lat/lng or null.
- geocodeUnitAddress callable Server Action returns {lat,lng,displayName} or {error}, server-side only with mandatory User-Agent.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-02-SUMMARY.md`.
</output>
