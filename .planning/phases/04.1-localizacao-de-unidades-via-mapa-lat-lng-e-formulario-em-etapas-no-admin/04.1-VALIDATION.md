---
phase: 04.1
slug: localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-17
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None detected — no Jest/Vitest in this repo. Existing pattern is ad hoc `tsx` verify scripts (`scripts/verify-auth.ts`, `scripts/verify-catalog.ts`, `scripts/verify-availability.ts`) run against the live Supabase instance. |
| **Config file** | none — Wave 0 creates `scripts/verify-units-location.ts` following the existing pattern |
| **Quick run command** | `npx tsx scripts/verify-units-location.ts` |
| **Full suite command** | same as quick run — this project has no separate full suite, only per-phase verify scripts |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit (schema/actions/geocoding):** Run `npx tsx scripts/verify-units-location.ts`
- **After every plan wave:** Run the same script + manual click-through of the wizard in `npm run dev`
- **Before `/gsd:verify-work`:** Full manual wizard walkthrough (create unit without location → badge shows; create unit with location → pin persists; edit existing Phase-3 unit → badge shows, completing Step 3 clears it)
- **Max feedback latency:** ~10 seconds (script) + manual click-through per wave

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 04.1-01-01 | 01 | 1 | Schema: lat/lng nullable on units | integration (live DB) | `npx tsx scripts/verify-units-location.ts` | ❌ W0 | ⬜ pending |
| 04.1-01-02 | 01 | 1 | createUnit/updateUnit persist lat/lng, accept omission | integration (live DB via Server Action) | same script | ❌ W0 | ⬜ pending |
| 04.1-0X-0X | TBD | TBD | Geocoding Server Action returns `{lat,lng}` for valid address, `{error}` for nonsense input | integration (live network call to Nominatim) | same script, separate assertion block | ❌ W0 | ⬜ pending |
| 04.1-0X-0X | TBD | TBD | Wizard step navigation, map drag, "Sem localização" badge rendering | manual | manual click-through during `/gsd:verify-work` | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*
*Exact task IDs to be finalized by gsd-planner — this map will be reconciled against the actual PLAN.md files.*

---

## Wave 0 Requirements

- [ ] `scripts/verify-units-location.ts` — covers migration correctness (lat/lng nullable, existing rows stay null), Server Action persistence of lat/lng (with and without value), and geocoding Server Action behavior (success + not-found cases). Follow the existing `scripts/verify-availability.ts` dotenv-walk-up + dynamic-import pattern.
- [ ] No new shared fixtures needed — reuse existing seeded restaurant/unit data from `scripts/seed.ts`.
- [ ] No test framework install — this project intentionally has no Jest/Vitest; stick with the established `tsx` ad hoc script convention.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Wizard step navigation (Próximo/Voltar, progress indicator) | D-02/D-03 | UI interaction, no test framework installed in this project | In `npm run dev`, open unit form, click through all 3 steps forward and back, confirm progress indicator updates and field values persist across steps |
| Map drag/pin placement + address search geocoding | D-04/D-05 | Requires real browser map interaction (Leaflet canvas), not unit-testable without a framework | In Step 3, type an address from Step 2, confirm map auto-centers; drag the pin, confirm new lat/lng captured on save |
| "Sem localização" badge rendering on units table | D-06/D-07 | Visual UI state, no test framework installed | Create a unit without completing Step 3, confirm badge appears in `/painel/unidades`; edit it and complete Step 3, confirm badge disappears |

---

## Validation Sign-Off

- [x] All tasks have automated verify (script) or Wave 0 dependency, except UI/interaction items explicitly marked manual-only above
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (schema + actions + geocoding all covered by `verify-units-location.ts`; only pure UI tasks are manual)
- [x] Wave 0 covers all MISSING references (`scripts/verify-units-location.ts` to be created)
- [x] No watch-mode flags
- [x] Feedback latency < 10s (script) — manual click-through is per-wave, not per-task
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
