---
phase: 04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin
plan: 01
subsystem: database
tags: [drizzle, postgres, leaflet, react-leaflet, migrations, supabase]

# Dependency graph
requires:
  - phase: 03-restaurant-admin-units-catalog-photos
    provides: units table (id, restaurantId, name, slug, address, whatsappNumber, hours)
provides:
  - units.lat / units.lng nullable numeric(10,7) number-mode columns
  - migration 0003_units_lat_lng.sql applied to live Supabase
  - leaflet@1.9.4, react-leaflet@5.0.0, @types/leaflet@1.9.21 installed
  - scripts/verify-units-location.ts (Wave 0 verification harness for this phase)
affects: [04.1-02, 04.1-03, 04.1-04, phase-5-nearest-unit]

# Tech tracking
tech-stack:
  added: [leaflet@1.9.4, react-leaflet@5.0.0, "@types/leaflet@1.9.21"]
  patterns:
    - "Drizzle numeric column with mode:'number' for lat/lng to avoid string round-trip (Pitfall 4)"
    - "tsx verify scripts: dotenv walk-up loader + dynamic imports after config() (matches verify-availability.ts)"
    - "Geocode assertions wrapped in try/catch keyed on 'Cannot find module' so Wave 0 script stays green before Plan 02 creates geocode.ts"

key-files:
  created:
    - src/db/migrations/0003_units_lat_lng.sql
    - scripts/verify-units-location.ts
  modified:
    - src/db/schema.ts
    - package.json
    - package-lock.json
    - src/db/migrations/meta/_journal.json
    - src/db/migrations/meta/0003_snapshot.json

key-decisions:
  - "lat/lng added as nullable numeric(10,7) mode:'number' columns on units, preserving Phase-3 rows with lat/lng=null (D-06/D-07)"
  - "drizzle-kit generate produced the expected filename 0003_units_lat_lng.sql with no renaming needed"
  - "verify-units-location.ts geocode block is skip-tolerant (Cannot find module) until Plan 02 adds src/lib/units/geocode.ts"

patterns-established:
  - "Wave 0 verify scripts for this phase live in scripts/verify-units-location.ts and are run after every schema/actions/geocoding task"

requirements-completed: []

# Metrics
duration: 12min
completed: 2026-06-17
---

# Phase 04.1 Plan 01: Data + Tooling Foundation for Unit Geolocation Summary

**Added nullable lat/lng numeric(10,7) number-mode columns to units via Drizzle migration 0003, installed leaflet/react-leaflet, and shipped the Wave 0 verify-units-location.ts harness — all green against live Supabase.**

## Performance

- **Duration:** 12 min
- **Started:** 2026-06-17T06:14:58Z (approx, after context gathering)
- **Completed:** 2026-06-17T06:27:00Z (approx)
- **Tasks:** 2 completed
- **Files modified:** 7 (2 created, 5 modified)

## Accomplishments
- `units` table now has nullable `lat`/`lng` `numeric(10,7)` columns with `mode: 'number'`, generated and applied as migration `src/db/migrations/0003_units_lat_lng.sql` against live Supabase via `drizzle-kit migrate`
- `leaflet@1.9.4`, `react-leaflet@5.0.0`, and `@types/leaflet@1.9.21` installed (exact versions verified against npm registry, matching React 19 peer deps)
- `scripts/verify-units-location.ts` created following the exact `verify-availability.ts` pattern (dotenv walk-up + dynamic imports after `config()`), proving: inserts without lat/lng persist as `null`, inserts with lat/lng round-trip as JS `number` (not string), and geocode assertions skip gracefully until Plan 02 creates `src/lib/units/geocode.ts`
- Registered `npm run verify-units-location` in `package.json`; script runs in ~3s and prints `ALL CHECKS PASSED` live against Supabase

## Task Commits

Each task was committed atomically:

1. **Task 1: Install leaflet/react-leaflet packages and add lat/lng columns to units schema** - `6e06fdd` (feat)
2. **Task 2: Create Wave 0 verification script scripts/verify-units-location.ts** - `f67defe` (feat)

**Plan metadata:** (pending — final commit below)

## Files Created/Modified
- `src/db/schema.ts` - Added `lat`/`lng` nullable `numeric(10,7)` `mode:'number'` columns to the `units` pgTable, no other lines changed
- `src/db/migrations/0003_units_lat_lng.sql` - Additive `ALTER TABLE "units" ADD COLUMN "lat" numeric(10, 7);` / `ADD COLUMN "lng" numeric(10, 7);`, generated by `drizzle-kit generate` and applied via `drizzle-kit migrate`
- `src/db/migrations/meta/_journal.json`, `src/db/migrations/meta/0003_snapshot.json` - drizzle-kit migration metadata for the new migration
- `package.json` / `package-lock.json` - Added `leaflet`, `react-leaflet` dependencies, `@types/leaflet` devDependency, and `verify-units-location` script
- `scripts/verify-units-location.ts` - Wave 0 integration verify script (LOCATION-NULL, LOCATION-PERSIST, GEOCODE-SUCCESS/NOTFOUND assertions)

## Decisions Made
- Followed the plan exactly: `drizzle-kit generate --name units_lat_lng` produced the expected filename `0003_units_lat_lng.sql` directly, so no manual renaming of the migration or journal entry was needed
- `mode: 'number'` confirmed mandatory and verified live — without it Drizzle would infer `lat: string | null`, which would break Phase 5 distance math; `LOCATION-PERSIST` assertion explicitly checks `typeof u2.lat === 'number'`

## Deviations from Plan

None - plan executed exactly as written. `drizzle-kit generate` named the migration exactly as expected (`0003_units_lat_lng.sql`), so the plan's fallback rename-or-note-actual-filename branch was not needed.

## Issues Encountered
- `npx tsc --noEmit` reports one expected error (`Cannot find module '../src/lib/units/geocode'`) from the dynamic import inside `verify-units-location.ts`'s try/catch geocode block. This is explicitly anticipated by the plan: `src/lib/units/geocode.ts` is created in Plan 02, and the script's runtime try/catch already handles the "Cannot find module" case gracefully (confirmed live: prints `GEOCODE SKIP (geocode.ts not created yet — Plan 02)` and still exits 0 with `ALL CHECKS PASSED`). Confirmed via `git stash -u` that `tsc --noEmit` was clean before this file existed, so this is the only new diagnostic and it is expected/acceptable per the plan's own acceptance criteria, not a regression.
- Pre-existing `npm run lint` issues (`react-hooks/incompatible-library` in `restaurant-form-dialog.tsx`, unrelated `no-unused-vars`/`no-explicit-any` in untracked debug scripts) are out of scope for this plan — not touched, not introduced by Task 1 or Task 2 changes.

## User Setup Required

None - no external service configuration required. Migration was applied directly to the live Supabase instance using the existing `DATABASE_URL` (Session Mode) credential already present in `.env`.

## Next Phase Readiness
- `units.lat`/`units.lng` columns exist live and are ready for Plan 02 (geocoding Server Action + `createUnit`/`updateUnit` persistence) and Plan 03/04 (map wizard UI)
- `leaflet`/`react-leaflet` are installed and ready for the map component work in later plans of this phase
- `scripts/verify-units-location.ts` is the standing Wave 0 regression check — subsequent plans in this phase should re-run `npm run verify-units-location` after every schema/actions/geocoding change; the GEOCODE block will start asserting for real once Plan 02 creates `src/lib/units/geocode.ts`

---
*Phase: 04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin*
*Completed: 2026-06-17*

## Self-Check: PASSED

- FOUND: src/db/migrations/0003_units_lat_lng.sql
- FOUND: scripts/verify-units-location.ts
- FOUND: src/db/schema.ts
- FOUND: commit 6e06fdd
- FOUND: commit f67defe
