---
phase: 04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin
verified: 2026-06-17T06:32:06Z
status: passed
score: 5/5 must-haves verified (all 4 plans), 8/8 locked decisions (D-01..D-08) confirmed
---

# Phase 04.1: Localização de unidades via mapa - lat/lng e formulário em etapas no admin Verification Report

**Phase Goal:** O admin do restaurante consegue definir a localização precisa de cada unidade através de um formulário em etapas com seleção interativa no mapa, fornecendo o dado de lat/lng que a Fase 5 depende para sugerir a unidade mais próxima ao cliente.
**Verified:** 2026-06-17T06:32:06Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (aggregated across 04.1-01..04.1-04 PLAN frontmatter)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | units table has nullable lat/lng numeric(10,7) columns, mode:'number' | VERIFIED | `src/db/schema.ts:42-43` — `lat: numeric('lat', { precision: 10, scale: 7, mode: 'number' })`, `lng` likewise, no `.notNull()` |
| 2 | Existing Phase-3 units remain valid with lat/lng = null after migration | VERIFIED | Migration is purely additive (`ALTER TABLE ... ADD COLUMN`, no backfill/NOT NULL); `verify-units-location.ts` LOCATION-NULL assertion passes live |
| 3 | leaflet, react-leaflet, @types/leaflet installed at verified versions | VERIFIED | `package.json`: `leaflet ^1.9.4`, `react-leaflet ^5.0.0` (deps), `@types/leaflet ^1.9.21` (devDeps) |
| 4 | Automated verify script proves migration correctness in ~10s | VERIFIED | Ran `npx tsx scripts/verify-units-location.ts` live — exits 0, `ALL CHECKS PASSED` in <10s |
| 5 | upsertUnitSchema accepts optional/nullable lat/lng (Step 3 skippable, D-07) | VERIFIED | `src/lib/units/schema.ts:18-19` — `.optional().nullable()` with range validation -90..90 / -180..180 |
| 6 | createUnit/updateUnit persist provided lat/lng, null when omitted | VERIFIED | `src/lib/units/actions.ts:56-57,100-101` — `lat: parsed.data.lat ?? null` in both insert and update |
| 7 | geocodeUnitAddress Server Action returns {lat,lng,displayName} or {error} | VERIFIED | `src/lib/units/actions.ts:138-148`; live-tested via verify script GEOCODE-SUCCESS/GEOCODE-NOTFOUND PASS |
| 8 | Nominatim called server-side only, with descriptive User-Agent | VERIFIED | `src/lib/units/geocode.ts:12` — `'User-Agent': 'CardapioBoaMidia/1.0 (contato@suporte.folkscomp.com)'`; geocode.ts has no `'use client'`, only imported from `actions.ts` (`'use server'`) |
| 9 | Interactive Leaflet map renders with OSM tiles + draggable marker | VERIFIED | `unit-location-map.tsx` — `MapContainer`/`TileLayer` (OSM url) / `Marker draggable` with `dragend` handler |
| 10 | Dragging marker reports new lat/lng to parent via onChange | VERIFIED | `unit-location-map.tsx:39-42` — `dragend: (e) => { const ll = e.target.getLatLng(); onChange(ll.lat, ll.lng) }` |
| 11 | Map loaded client-only (next/dynamic ssr:false), SSR never imports leaflet | VERIFIED | `unit-location-map-loader.tsx` — `dynamic(() => import('./unit-location-map')..., { ssr: false })`; `npm run build` succeeded with zero `window is not defined` errors |
| 12 | Default marker icon renders correctly under Turbopack | VERIFIED | `unit-location-map.tsx:12-17` — `delete L.Icon.Default.prototype._getIconUrl` + `mergeOptions` with PNG imports guarded for both string and `{src}` resolution |
| 13 | Unit form is a 3-step wizard inside one Dialog: Básico, Contato/Horário, Localização (D-02) | VERIFIED | `unit-form-dialog.tsx:28-38` `STEP_FIELDS`/`STEP_TITLES`; steps rendered conditionally via `display: step===N` |
| 14 | Próximo/Voltar navigation with visible "Etapa N de 3" progress indicator (D-03) | VERIFIED | `unit-form-dialog.tsx:188-198` (`Etapa {step} de 3` + 3 dots), `:298-319` (footer buttons gated by step) |
| 15 | Step 3 shows interactive map; auto-geocodes Step-2 address on entry (D-05); manual address-search (D-04) | VERIFIED | `unit-form-dialog.tsx:137-144` (`useEffect` auto-geocode once via `autoGeoDone` ref), `:268-282` (Buscar button + input) |
| 16 | Admin can save a unit without placing a pin (location optional, D-07) | VERIFIED | `STEP_FIELDS[3] = ['lat','lng']` both optional/nullable in schema; `goNext`/submit do not require lat/lng to be set; `createUnit`/`updateUnit` accept `lat?: number\|null` |
| 17 | Units with null lat/lng show discreet "Sem localização" badge in units table (D-06) | VERIFIED | `unit-table.tsx:44-51` — `Badge variant="outline"` rendered when `unit.lat == null \|\| unit.lng == null` |

**Score:** 17/17 truths verified (0 failed, 0 uncertain)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/db/schema.ts` | units table with lat/lng numeric(10,7) nullable + mode:'number' | VERIFIED | Lines 42-43, exact match to plan spec |
| `src/db/migrations/0003_units_lat_lng.sql` | Additive ALTER TABLE adding lat/lng | VERIFIED | 2 `ADD COLUMN` statements, no destructive/backfill SQL |
| `scripts/verify-units-location.ts` | Wave 0 integration verification | VERIFIED | Ran live — `ALL CHECKS PASSED`, exit 0, ~3-4s including 1.1s Nominatim rate-limit pause |
| `src/lib/units/schema.ts` | upsertUnitSchema + UpsertUnitInput w/ lat/lng | VERIFIED | `lat: z.number()...optional().nullable()` present, whatsapp logic untouched |
| `src/lib/units/geocode.ts` | server-only Nominatim wrapper `geocodeAddress()` | VERIFIED | Mandatory User-Agent present, `lon`→`lng` mapping correct |
| `src/lib/units/actions.ts` | createUnit/updateUnit persist lat/lng + geocodeUnitAddress action | VERIFIED | Both mutations include `lat`/`lng` in values/set; `geocodeUnitAddress` guarded by `getCurrentAdmin()` |
| `src/app/painel/unidades/unit-location-map.tsx` | Real Leaflet map w/ draggable marker + recenter | VERIFIED | `MapContainer`, `Recenter` (`useMap`+`setView`), marker-icon fix present |
| `src/app/painel/unidades/unit-location-map-loader.tsx` | next/dynamic ssr:false wrapper | VERIFIED | `ssr: false`, loading fallback "Carregando mapa…" |
| `src/app/painel/unidades/page.tsx` | UnitRow incl. lat/lng; query selects lat,lng | VERIFIED | Interface + supabase `.select(...)` + null-safe `Number()` coercion |
| `src/app/painel/unidades/unit-table.tsx` | "Sem localização" badge for null-location rows | VERIFIED | Badge rendered conditionally, `Badge` import present |
| `src/app/painel/unidades/unit-form-dialog.tsx` | 3-step wizard w/ map step + auto-geocode + manual search | VERIFIED | All wizard mechanics, map embed, geocode wiring present and building cleanly |

All 11 artifacts: exists = yes, substantive = yes (no stubs/placeholders), level checked individually below for wiring.

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `src/db/schema.ts` | `src/db/migrations/0003_units_lat_lng.sql` | `drizzle-kit generate` | WIRED | Migration SQL matches schema column defs exactly (numeric(10,7)) |
| `src/lib/units/actions.ts` | `src/lib/units/geocode.ts` | `import { geocodeAddress } from './geocode'` | WIRED | `actions.ts:10` imports it; `geocodeUnitAddress` action calls `geocodeAddress(q)` at line 145 |
| `src/lib/units/actions.ts` | `units.lat / units.lng` | `db.insert/update .values({ lat, lng })` | WIRED | Both `createUnit` (line 56-57) and `updateUnit` (line 100-101) set `lat`/`lng` from `parsed.data` |
| `unit-location-map-loader.tsx` | `unit-location-map.tsx` | `next/dynamic` import | WIRED | `dynamic(() => import('./unit-location-map').then(m => m.UnitLocationMap), { ssr: false })` |
| `unit-form-dialog.tsx` | `unit-location-map-loader.tsx` | `import { UnitLocationMap } from './unit-location-map-loader'` | WIRED | Line 10; confirmed NOT importing the raw map file directly (avoids SSR break) |
| `unit-form-dialog.tsx` | `geocodeUnitAddress` Server Action | import + call on Step-3 entry and Buscar click | WIRED | Imported line 8 from `@/lib/units/actions`; called in `runGeocode()` (auto on step 3 entry via `useEffect`, and on Buscar button click) |
| `unit-table.tsx` | `unit.lat / unit.lng` null check | conditional Badge render | WIRED | `(unit.lat == null \|\| unit.lng == null) && <Badge>` |

All 7 key links: WIRED, none ORPHANED/PARTIAL/NOT_WIRED.

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|---------------------|--------|
| `unit-table.tsx` (Badge) | `unit.lat`/`unit.lng` | `page.tsx` Supabase query `.select('id, name, address, whatsapp_number, hours, lat, lng')` | Yes — real column selected from live `units` table, not hardcoded | FLOWING |
| `unit-form-dialog.tsx` (Step 3 map) | `lat`/`lng` form state (`watch`) | RHF `defaultValues` seeded from `unit.lat`/`unit.lng` (edit mode) or geocode/drag updates (`setValue`) | Yes — DB-seeded on edit, live Nominatim/drag on create | FLOWING |
| `unit-location-map.tsx` (Marker) | `lat`/`lng` props | Passed down from `unit-form-dialog.tsx` (`mapLat`/`mapLng` = form value or `BR_CENTER` fallback) | Yes — concrete numbers always, fallback is intentional per D-spec (not a stub) | FLOWING |
| `geocodeUnitAddress` action | Nominatim API response | Live HTTP fetch in `geocode.ts`, no static/mocked return | Yes — confirmed live in verify script (`GEOCODE-SUCCESS PASS` against real "Avenida Paulista" query) | FLOWING |

No hollow/disconnected data paths found.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Wave 0 verify script (schema + persistence + geocoding) against live Supabase | `npx tsx scripts/verify-units-location.ts` | `LOCATION-NULL PASS`, `LOCATION-PERSIST PASS`, `GEOCODE-SUCCESS PASS`, `GEOCODE-NOTFOUND PASS`, `ALL CHECKS PASSED`, exit 0 | PASS |
| TypeScript project-wide type-check | `npx tsc --noEmit` | "TypeScript: No errors found" | PASS |
| Production build (validates Pitfall 2 — no SSR window/document crash from Leaflet) | `npm run build` | Compiled successfully, all routes generated incl. `/painel/unidades`, no `window is not defined` errors | PASS |
| Lint | `npm run lint` | 1 errors, 10 warnings — none in phase-touched files are new blockers (see Anti-Patterns) | PASS (no blockers) |

### Requirements Coverage

No formal REQ-IDs exist for this phase (urgent inserted phase, `requirements: []` in all 4 PLAN.md frontmatter, confirmed). Verified instead against the 8 locked decisions in `04.1-CONTEXT.md`:

| Decision | Description | Status | Evidence |
|----------|--------------|--------|----------|
| D-01 | Leaflet + OSM (no API key) + Nominatim geocoding (free) | SATISFIED | `leaflet`/`react-leaflet` deps, OSM tile URL `https://{s}.tile.openstreetmap.org/...`, Nominatim `https://nominatim.openstreetmap.org/search` — no API keys anywhere |
| D-02 | 3-step wizard in one Dialog: Básico / Contato-Horário / Localização | SATISFIED | `STEP_TITLES` = `{1: 'Básico', 2: 'Contato e Horário', 3: 'Localização'}`, single `<Dialog>`/`<form>` |
| D-03 | Próximo/Voltar footer nav + "Etapa N de 3" progress indicator | SATISFIED | `unit-form-dialog.tsx:188-198` (indicator) + `:298-319` (footer nav) |
| D-04 | Address search field on Localização step via Nominatim, centers map; draggable pin for manual adjust | SATISFIED | Search `Input` + "Buscar" button calling `runGeocode`; `Marker draggable` with `dragend` |
| D-05 | Auto-geocode Step-2 address on entering Step 3, centering map | SATISFIED | `useEffect` keyed on `step===3 && !autoGeoDone.current && lat==null && lng==null` triggers `runGeocode(getValues('address'))` once |
| D-06 | Old (Phase-3) units with null lat/lng show discreet "Sem localização" badge in `/painel/unidades` table | SATISFIED | `unit-table.tsx` `Badge variant="outline"` conditional render; nothing blocks editing/using these units |
| D-07 | Localização step optional for new units; saving without a pin leaves lat/lng null + same badge | SATISFIED | `lat`/`lng` `.optional().nullable()` in zod schema; `goNext`/submit have no requirement on Step 3 fields; verified live via LOCATION-NULL assertion |
| D-08 | (Phase 5 consequence) units with null lat/lng simply ignored by nearest-unit logic — no error/fallback | N/A TO THIS PHASE | This is a Phase 5 implementation concern; D-08 only requires that THIS phase produce nullable lat/lng without forcing population — confirmed (D-06/D-07 evidence). No Phase 5 code expected or found in this phase's scope, correctly out of bounds per the Phase Boundary note ("não a implementa") |

8/8 decisions accounted for (7 directly implemented + verified, 1 correctly out-of-scope and contractually satisfied by D-06/D-07's null-safety).

No orphaned requirements — REQUIREMENTS.md has no Phase 04.1 entries (confirmed by phase being an inserted/urgent phase with no REQ-ID mapping), consistent with the verification instructions.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `src/app/painel/unidades/unit-form-dialog.tsx` | n/a (whole file, react-hook-form usage) | ESLint `react-hooks/incompatible-library` warning | Info | Pre-existing project-wide pattern (also fires 9x in unrelated files like `restaurant-form-dialog.tsx` from Phase 2/3); not a regression introduced by this phase, not a functional defect — build and type-check both pass clean |

No TODO/FIXME/PLACEHOLDER comments, no empty stub handlers, no hardcoded-empty data paths found in any of the 11 phase-touched files. The only `placeholder=` matches are legitimate HTML input placeholder attributes (UX copy), and the only `return null` is the intended "address not found" contract in `geocode.ts`.

### Human Verification Required

None strictly required to confirm goal achievement — all D-01..D-08 decisions and all must-haves were verifiable via code inspection, live DB round-trip, live Nominatim call, `tsc`, and `next build`. The following are optional polish/UX confirmations the team may still want to eyeball, per `04.1-VALIDATION.md`'s "Manual-Only Verifications" table:

### 1. Wizard step navigation feel

**Test:** In `npm run dev`, open "Nova Unidade", click through all 3 steps forward and back.
**Expected:** Progress indicator updates, field values persist across steps, Voltar/Próximo behave intuitively.
**Why human:** Pure UI/UX feel, no test framework installed in this project (by design, per VALIDATION.md).

### 2. Map drag + address search visual behavior

**Test:** In Step 3, type a Step-2 address, confirm map auto-centers on entry; drag the pin and confirm the value sticks after save.
**Expected:** Map recenters smoothly, dragging the pin is responsive, saved unit reflects dragged coordinates on next edit.
**Why human:** Requires real browser Leaflet canvas interaction; logic is verified (dragend handler, setValue calls) but the actual drag UX is not automatable here.

### 3. "Sem localização" badge visual styling

**Test:** View `/painel/unidades` with a mix of geocoded and non-geocoded units.
**Expected:** Badge looks "discreet" (outline variant, muted text) per D-06's intent, doesn't visually dominate the row.
**Why human:** Subjective visual judgment on "discreet" styling.

### Gaps Summary

No gaps found. All 4 plans' must-haves (truths, artifacts, key_links) are verified present, substantive, and wired in the live codebase — not just claimed in SUMMARY.md files. The Wave 0 verify script was re-run live during this verification and passed all 5 assertions including the live Nominatim geocoding round-trip. `npx tsc --noEmit` and `npm run build` both pass clean, confirming the SSR-isolation pitfall (Leaflet touching `window`) was correctly avoided. All 8 locked decisions (D-01 through D-08) have direct code evidence; D-08 is correctly scoped as a Phase-5 consequence that this phase satisfies the precondition for (nullable lat/lng, no forced population) without overreaching into Phase 5's implementation.

---

*Verified: 2026-06-17T06:32:06Z*
*Verifier: Claude (gsd-verifier)*
