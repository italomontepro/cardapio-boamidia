---
phase: 04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin
plan: 04
type: execute
wave: 4
depends_on: ["04.1-02", "04.1-03"]
files_modified:
  - src/app/painel/unidades/page.tsx
  - src/app/painel/unidades/unit-table.tsx
  - src/app/painel/unidades/unit-form-dialog.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The unit form is a 3-step wizard inside one Dialog: Básico, Contato/Horário, Localização (D-02)"
    - "Próximo/Voltar navigation with a visible 'Etapa N de 3' progress indicator (D-03)"
    - "Step 3 shows the interactive map; on entry it auto-geocodes the Step-2 address (D-05) and offers a manual address-search (D-04)"
    - "Admin can save a unit without placing a pin (location optional, D-07)"
    - "Units with null lat/lng show a discreet 'Sem localização' badge in the units table (D-06)"
  artifacts:
    - path: "src/app/painel/unidades/page.tsx"
      provides: "UnitRow includes lat/lng; query selects lat,lng"
      contains: "lat"
    - path: "src/app/painel/unidades/unit-table.tsx"
      provides: "'Sem localização' badge for null-location rows"
      contains: "Sem localização"
    - path: "src/app/painel/unidades/unit-form-dialog.tsx"
      provides: "3-step wizard with map step + auto-geocode + manual search"
      contains: "Etapa"
  key_links:
    - from: "src/app/painel/unidades/unit-form-dialog.tsx"
      to: "src/app/painel/unidades/unit-location-map-loader.tsx"
      via: "import UnitLocationMap (dynamic loader, NOT the raw map)"
      pattern: "from './unit-location-map-loader'"
    - from: "src/app/painel/unidades/unit-form-dialog.tsx"
      to: "geocodeUnitAddress Server Action"
      via: "import + call on Step 3 entry and Buscar click"
      pattern: "geocodeUnitAddress"
    - from: "src/app/painel/unidades/unit-table.tsx"
      to: "unit.lat / unit.lng null check"
      via: "conditional Badge render"
      pattern: "lat == null"
---

<objective>
Deliver the user-facing experience (D-02 through D-07): refactor the single-step unit Dialog into a 3-step wizard (Básico → Contato/Horário → Localização) with progress indicator and Próximo/Voltar navigation, embed the interactive map in Step 3 with auto-geocoding of the Step-2 address (D-05) and a manual address search (D-04), make location optional (D-07), and add the discreet "Sem localização" badge to the units table (D-06).

Purpose: This is the whole point of the phase — give the admin an iFood-style, visually pleasant way to pin each unit's exact location, while not blocking quick unit creation and nudging old units to fill in their coordinates.
Output: rewritten unit-form-dialog.tsx (wizard), updated page.tsx (lat/lng in UnitRow + query), updated unit-table.tsx (badge).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-CONTEXT.md
@.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md

<interfaces>
<!-- Map widget (from Plan 03) — import from the LOADER, never the raw map file: -->
import { UnitLocationMap } from './unit-location-map-loader'
// <UnitLocationMap lat={number} lng={number} onChange={(lat, lng) => void} />

<!-- Server Action (from Plan 02): -->
import { geocodeUnitAddress } from '@/lib/units/actions'
// geocodeUnitAddress(address: string): Promise<{ success: true; lat; lng; displayName } | { error: string }>

<!-- Schema (from Plan 02) — single schema, gate steps via form.trigger([...fields]): -->
// upsertUnitSchema now has optional/nullable lat, lng. type UpsertUnitInput includes lat?, lng?.

<!-- CURRENT UnitRow (src/app/painel/unidades/page.tsx) — extend with lat/lng: -->
export interface UnitRow { id; name; address: string|null; whatsappNumber: string; hours: string|null }
// page query currently: .from('units').select('id, name, address, whatsapp_number, hours').order('name')

<!-- CURRENT form (unit-form-dialog.tsx): single <form onSubmit={handleSubmit(onSubmit)}> with 4 fields
     (name, address, whatsappNumber, hours), useForm<UpsertUnitInput> + zodResolver(upsertUnitSchema),
     calls createUnit/updateUnit, router.refresh() on success. Dialog/DialogContent/DialogFooter from
     '@/components/ui/dialog', Label/Input/Button shadcn. getDefaultWhatsapp() formats existing number. -->

<!-- Badge (src/components/ui/badge.tsx): <Badge variant="outline">…</Badge> ; variant="outline" = discreet (D-06). -->

<!-- Brazil-center fallback for the map when no coords yet: lat -14.235, lng -51.9253, zoom 4 (RESEARCH OQ1). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend UnitRow + units query with lat/lng (page.tsx) and add 'Sem localização' badge (unit-table.tsx)</name>
  <files>src/app/painel/unidades/page.tsx, src/app/painel/unidades/unit-table.tsx</files>
  <read_first>
    - src/app/painel/unidades/page.tsx (current UnitRow interface + supabase select + row mapping)
    - src/app/painel/unidades/unit-table.tsx (current table; where to add the badge)
    - src/components/ui/badge.tsx (Badge component + variant="outline" for the discreet look)
  </read_first>
  <action>
    PART A — `src/app/painel/unidades/page.tsx`:
    1. Add `lat: number | null` and `lng: number | null` to the `UnitRow` interface (after `hours`).
    2. Change the supabase select to include the columns: `.select('id, name, address, whatsapp_number, hours, lat, lng')`.
    3. In the `.map((row) => ({...}))`, add `lat: row.lat ?? null,` and `lng: row.lng ?? null,`. (Supabase returns numeric(10,7) as number via PostgREST, but coerce defensively: if a string sneaks through, use `row.lat == null ? null : Number(row.lat)`.)

    PART B — `src/app/painel/unidades/unit-table.tsx`:
    1. Add `import { Badge } from '@/components/ui/badge'`.
    2. In the Nome cell (the `<TableCell className="font-medium">{unit.name}</TableCell>`), render the unit name plus, when location is missing, the badge — change that cell to:
       ```
       <TableCell className="font-medium">
         <div className="flex items-center gap-2">
           <span>{unit.name}</span>
           {(unit.lat == null || unit.lng == null) && (
             <Badge variant="outline" className="text-muted-foreground font-normal">Sem localização</Badge>
           )}
         </div>
       </TableCell>
       ```
       Do not add a new column (keeps the table compact and the badge "discreto" per D-06). Leave all other cells unchanged.
  </action>
  <verify>
    <automated>grep -q "lat, lng" src/app/painel/unidades/page.tsx && grep -q "lat: number | null" src/app/painel/unidades/page.tsx && grep -q "Sem localização" src/app/painel/unidades/unit-table.tsx && grep -q "unit.lat == null" src/app/painel/unidades/unit-table.tsx && grep -q "import { Badge }" src/app/painel/unidades/unit-table.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` `UnitRow` includes `lat: number | null` and `lng: number | null`
    - `page.tsx` supabase select string includes `lat, lng`
    - `page.tsx` row mapping sets `lat`/`lng` (null-safe)
    - `unit-table.tsx` imports `Badge` and renders `<Badge variant="outline" ...>Sem localização</Badge>` only when `unit.lat == null || unit.lng == null`
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Units listing carries lat/lng; rows missing coordinates display a discreet outline "Sem localização" badge next to the name (D-06).</done>
</task>

<task type="auto">
  <name>Task 2: Refactor unit-form-dialog.tsx into a 3-step wizard with progress indicator and step-gated navigation (D-02/D-03/D-07)</name>
  <files>src/app/painel/unidades/unit-form-dialog.tsx</files>
  <read_first>
    - src/app/painel/unidades/unit-form-dialog.tsx (the file being rewritten; preserve getDefaultWhatsapp, createUnit/updateUnit calls, error-mapping in onSubmit, Dialog structure, router.refresh)
    - src/app/painel/unidades/page.tsx (UnitRow now has lat/lng after Task 1 — edit mode defaults read them)
    - src/lib/units/schema.ts (upsertUnitSchema field names: name, address, whatsappNumber, hours, lat, lng)
    - .planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md ("Pattern 3: Multi-step wizard with one useForm + step-scoped trigger()")
  </read_first>
  <action>
    Rewrite `src/app/painel/unidades/unit-form-dialog.tsx` keeping the SAME single `<form onSubmit={handleSubmit(onSubmit)}>`, SAME `useForm<UpsertUnitInput>` + `zodResolver(upsertUnitSchema)`, SAME createUnit/updateUnit calls and error mapping, SAME getDefaultWhatsapp helper. Add wizard behavior:

    1. Step state: `const [step, setStep] = useState(1)` (1..3). Reset to 1 whenever the Dialog opens (in the onClick that opens it, and on close-reset).

    2. defaultValues: in EDIT mode also seed `lat: unit.lat ?? undefined, lng: unit.lng ?? undefined`. Add lat/lng to create defaults as `lat: undefined, lng: undefined`. Register lat/lng so RHF tracks them even though they have no <input>: call `register('lat')` and `register('lng')` once (hidden inputs) OR rely on `setValue` — use hidden inputs: `<input type="hidden" {...register('lat', { valueAsNumber: true })} />` and same for lng, placed inside the form.

    3. Step field map + navigation (RESEARCH Pattern 3):
       ```
       const STEP_FIELDS = {
         1: ['name'] as const,
         2: ['whatsappNumber', 'address', 'hours'] as const,
         3: ['lat', 'lng'] as const,
       }
       async function goNext() {
         const valid = await form.trigger(STEP_FIELDS[step as 1 | 2 | 3])
         if (!valid) return
         setStep((s) => Math.min(s + 1, 3))
       }
       function goBack() { setStep((s) => Math.max(s - 1, 1)) }
       ```
       (Expose `trigger`, `setValue`, `watch`, `getValues` from useForm.)

    4. Progress indicator (D-03): above the step content render `Etapa {step} de 3` text plus 3 dots:
       ```
       <div className="flex items-center gap-2">
         <span className="text-sm font-medium text-muted-foreground">Etapa {step} de 3</span>
         <div className="ml-auto flex gap-1.5">
           {[1,2,3].map((n) => (
             <span key={n} className={`size-2 rounded-full ${n <= step ? 'bg-primary' : 'bg-muted'}`} />
           ))}
         </div>
       </div>
       ```
       Step titles: 1 = "Básico", 2 = "Contato e Horário", 3 = "Localização". Use these in DialogTitle or a subheading.

    5. Render only the current step's fields (keep all fields mounted but hidden, OR conditionally render — conditional render is fine since RHF keeps values in its store; use `style={{ display: step===N ? 'block':'none' }}` wrappers to keep inputs mounted and avoid re-mount focus loss):
       - Step 1: the existing Nome field.
       - Step 2: the existing WhatsApp, Endereço, Horários fields.
       - Step 3: the map block (built in Task 3) — for THIS task, leave a placeholder `<div data-step3-map />` that Task 3 replaces.

    6. Footer buttons (D-03): replace the single footer with step-aware buttons, all map/nav buttons `type="button"`, only the final Salvar `type="submit"`:
       ```
       <DialogFooter>
         {step > 1 && <Button type="button" variant="outline" onClick={goBack}>Voltar</Button>}
         {step < 3 && <Button type="button" onClick={goNext}>Próximo</Button>}
         {step === 3 && <Button type="submit" disabled={isSubmitting}>{isCreate ? 'Criar Unidade' : 'Salvar Alterações'}</Button>}
       </DialogFooter>
       ```
       Remove the old single "Cancelar/Criar" footer. (A Cancelar can stay on step 1 as an outline button if desired, optional.)

    7. onSubmit: unchanged except it now also carries lat/lng from form data automatically (they're in UpsertUnitInput). Ensure the createUnit/updateUnit calls pass the full `data` object (they already do). Add lat/lng to the error-mapping block defensively is NOT required (they have no user-facing field). After success keep `setOpen(false); reset(); router.refresh()` and also `setStep(1)`.

    IMPORTANT: This task wires the wizard shell + steps 1 and 2 fully. Step 3's map content is a placeholder `<div data-step3-map />` here — Task 3 fills it in. Steps 1/2 navigation, progress indicator, and save-without-location MUST work after this task.
  </action>
  <verify>
    <automated>grep -q "Etapa" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "STEP_FIELDS" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "form.trigger" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "goBack" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "Próximo" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "data-step3-map" src/app/painel/unidades/unit-form-dialog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File contains `STEP_FIELDS` with `1: ['name']`, `2: ['whatsappNumber', 'address', 'hours']`, `3: ['lat', 'lng']`
    - Contains `form.trigger(` (or `trigger(`) used in `goNext`, and a `goBack` function
    - Renders `Etapa {step} de 3` and 3 progress dots
    - Footer shows Voltar (step>1), Próximo (step<3, type="button"), and the submit button only at step===3
    - Hidden inputs register `lat` and `lng` with `valueAsNumber: true`
    - Contains placeholder `data-step3-map` (replaced in Task 3)
    - `npx tsc --noEmit` clean; only the final-step button is `type="submit"`
  </acceptance_criteria>
  <done>The Dialog is a 3-step wizard: Básico → Contato/Horário → Localização, with progress indicator, Próximo/Voltar (step-validated via trigger), and the ability to submit without location (D-07). Step 3 has a placeholder for the map.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Step 3 map: embed UnitLocationMap, auto-geocode Step-2 address on entry (D-05) + manual address search (D-04)</name>
  <files>src/app/painel/unidades/unit-form-dialog.tsx</files>
  <read_first>
    - src/app/painel/unidades/unit-form-dialog.tsx (the wizard shell from Task 2; replace the data-step3-map placeholder)
    - src/app/painel/unidades/unit-location-map-loader.tsx (the dynamic UnitLocationMap to import — NEVER import unit-location-map.tsx directly)
    - src/lib/units/actions.ts (geocodeUnitAddress Server Action signature)
    - .planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-RESEARCH.md (Pitfall 3 rate-limit: geocode only on Step-3 entry + explicit Buscar click, never per-keystroke)
  </read_first>
  <action>
    In `src/app/painel/unidades/unit-form-dialog.tsx`, add imports:
    `import { UnitLocationMap } from './unit-location-map-loader'`
    `import { geocodeUnitAddress } from '@/lib/units/actions'`
    `import { useEffect, useRef } from 'react'` (merge with existing react import)

    Replace the `<div data-step3-map />` placeholder with the Step 3 location UI. Keep the `data-step3-map` attribute on the wrapper div so the grep check still passes. Implement:

    1. Read current coords from form: `const lat = watch('lat'); const lng = watch('lng')`. Define the Brazil-center fallback constants: `const BR_CENTER = { lat: -14.235, lng: -51.9253 }` (RESEARCH OQ1). The map always renders with concrete numbers: `const mapLat = lat ?? BR_CENTER.lat; const mapLng = lng ?? BR_CENTER.lng`.

    2. Local state: `const [searchValue, setSearchValue] = useState(() => getValues('address') ?? '')`, `const [geoStatus, setGeoStatus] = useState<string | null>(null)` (for "Endereço não encontrado…" / "Buscando…" messages), `const [geoLoading, setGeoLoading] = useState(false)`.

    3. A reusable `async function runGeocode(query: string)`:
       ```
       const q = query.trim()
       if (!q) { setGeoStatus('Digite um endereço para buscar.'); return }
       setGeoLoading(true); setGeoStatus('Buscando endereço…')
       const res = await geocodeUnitAddress(q)
       setGeoLoading(false)
       if ('error' in res) {
         setGeoStatus(res.error)  // e.g. "Endereço não encontrado. Ajuste o pino manualmente no mapa."
         return
       }
       setValue('lat', res.lat, { shouldValidate: true })
       setValue('lng', res.lng, { shouldValidate: true })
       setGeoStatus(null)
       ```

    4. Auto-geocode on Step 3 entry (D-05) — fire ONCE when step becomes 3 AND no coords yet, using a ref guard so it doesn't refire on every render (Pitfall 3 rate-limit):
       ```
       const autoGeoDone = useRef(false)
       useEffect(() => {
         if (step === 3 && !autoGeoDone.current && lat == null && lng == null) {
           autoGeoDone.current = true
           const addr = getValues('address')
           if (addr && addr.trim()) runGeocode(addr)
         }
       }, [step])  // intentionally only depends on step
       ```
       Reset `autoGeoDone.current = false` when the Dialog closes (in the same place step resets to 1).

    5. Search field UI (D-04) — input + explicit "Buscar" button (button-triggered, NOT live-as-you-type, per Pitfall 3):
       ```
       <div className="flex gap-2">
         <Input value={searchValue} onChange={(e) => setSearchValue(e.target.value)}
           placeholder="Buscar endereço no mapa" />
         <Button type="button" variant="outline" disabled={geoLoading}
           onClick={() => runGeocode(searchValue)}>Buscar</Button>
       </div>
       {geoStatus && <p className="text-sm text-muted-foreground">{geoStatus}</p>}
       ```

    6. The map itself (drag updates the hidden lat/lng):
       ```
       <UnitLocationMap lat={mapLat} lng={mapLng} onChange={(newLat, newLng) => {
         setValue('lat', newLat, { shouldValidate: true })
         setValue('lng', newLng, { shouldValidate: true })
       }} />
       <p className="text-xs text-muted-foreground">
         Arraste o pino para ajustar a posição exata. Esta etapa é opcional — você pode salvar sem marcar.
       </p>
       ```

    Do NOT geocode on keystroke and do NOT loop — only Step-3-entry (once) and Buscar-click trigger Nominatim, honoring the 1 req/sec policy.
  </action>
  <verify>
    <automated>grep -q "from './unit-location-map-loader'" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "geocodeUnitAddress" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "UnitLocationMap" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "Buscar" src/app/painel/unidades/unit-form-dialog.tsx && grep -q "autoGeoDone" src/app/painel/unidades/unit-form-dialog.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Imports `UnitLocationMap` from `'./unit-location-map-loader'` (NOT from `'./unit-location-map'`)
    - Imports and calls `geocodeUnitAddress`
    - Renders `<UnitLocationMap lat=... lng=... onChange=...>` whose onChange calls `setValue('lat'...)` and `setValue('lng'...)`
    - Has a "Buscar" button calling a geocode function (button-triggered, not per-keystroke) and a status message area
    - Auto-geocodes once on Step 3 entry via a `useRef` guard (`autoGeoDone`) depending on `step`
    - Uses Brazil-center fallback `-14.235 / -51.9253` when lat/lng are null
    - `npm run build` succeeds with NO `window is not defined` / `document is not defined` error (Pitfall 2 verified end-to-end)
  </acceptance_criteria>
  <done>Step 3 shows the interactive map; entering it auto-centers on the Step-2 address (D-05), a Buscar field re-geocodes on demand (D-04), dragging the pin updates the saved coordinates, and the step remains optional (D-07). Production build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` completes with no SSR `window`/`document` errors.
- `npx tsc --noEmit` / `npm run lint` clean.
- grep confirms: 3-step wizard ("Etapa", STEP_FIELDS, trigger, Próximo/Voltar), map loader import, geocodeUnitAddress, "Sem localização" badge.
- Manual (per VALIDATION.md): create unit without location → badge appears; create with location → pin persists; edit Phase-3 unit → badge shows, completing Step 3 clears it.
</verification>

<success_criteria>
- 3-step wizard inside one Dialog with progress indicator and Próximo/Voltar (D-02/D-03).
- Step 3 interactive map: auto-geocode on entry (D-05) + manual Buscar (D-04) + draggable pin.
- Location optional — save without a pin works (D-07).
- "Sem localização" badge on null-location units (D-06).
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-localizacao-de-unidades-via-mapa-lat-lng-e-formulario-em-etapas-no-admin/04.1-04-SUMMARY.md`.
</output>
