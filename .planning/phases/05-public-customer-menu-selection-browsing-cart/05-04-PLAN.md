---
phase: 05-public-customer-menu-selection-browsing-cart
plan: 04
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - src/app/r/[restaurantSlug]/[unitSlug]/layout.tsx
  - src/app/r/[restaurantSlug]/[unitSlug]/page.tsx
  - src/app/r/[restaurantSlug]/[unitSlug]/menu-view.tsx
  - src/app/r/[restaurantSlug]/[unitSlug]/product-dialog.tsx
  - src/app/r/[restaurantSlug]/[unitSlug]/cart-fab.tsx
  - src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx
autonomous: true
requirements: [MENU-02, MENU-03, MENU-04, MENU-05, CART-01, CART-02]
must_haves:
  truths:
    - "After picking a unit, the menu shows categories as Tabs in admin sortOrder with name, description, photo, price (MENU-02)"
    - "Only products available at the selected unit appear; empty categories are absent (MENU-03 via Plan 01 query)"
    - "Featured products appear in a 'Destaques' strip above the tabs AND inside their category (D-07/MENU-04)"
    - "Prices render in pt-BR R$ format (MENU-05) and the layout is mobile-first"
    - "Current unit name is always visible (sticky header indicator) — D-05"
    - "Tapping a product opens a Dialog with photo, description, qty stepper, notes; 'Adicionar ao carrinho' adds and closes — D-09/CART-01"
    - "A floating 'Ver carrinho — N itens' button opens a bottom Sheet — D-08"
    - "Inside the Sheet each line has inline +/- stepper and remove; subtotal shown in R$ — D-11/CART-02"
    - "Invalid unit slug (or restaurant mismatch) renders not-found — D-12"
  artifacts:
    - path: "src/app/r/[restaurantSlug]/[unitSlug]/layout.tsx"
      provides: "Resolves restaurant+unit IDs, wraps children in CartProvider with cart:<restaurantId>:<unitId> key"
      min_lines: 15
    - path: "src/app/r/[restaurantSlug]/[unitSlug]/page.tsx"
      provides: "Server Component: getMenuForUnit, 404 on bad unit, render MenuView + CartFab"
      min_lines: 20
    - path: "src/app/r/[restaurantSlug]/[unitSlug]/menu-view.tsx"
      provides: "Tabs, Destaques strip, sticky unit indicator, product cards"
      min_lines: 60
    - path: "src/app/r/[restaurantSlug]/[unitSlug]/product-dialog.tsx"
      provides: "Product detail Dialog with stepper + notes + add-to-cart"
      min_lines: 40
    - path: "src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx"
      provides: "Bottom Sheet: line items, inline steppers, remove, subtotal"
      min_lines: 40
    - path: "src/app/r/[restaurantSlug]/[unitSlug]/cart-fab.tsx"
      provides: "Floating cart button showing item count, opens the Sheet"
      min_lines: 15
  key_links:
    - from: "src/app/r/[restaurantSlug]/[unitSlug]/layout.tsx"
      to: "src/app/r/[restaurantSlug]/[unitSlug]/cart-provider.tsx"
      via: "CartProvider storageKey=cart:restaurantId:unitId"
      pattern: "CartProvider"
    - from: "src/app/r/[restaurantSlug]/[unitSlug]/page.tsx"
      to: "src/lib/menu/queries.ts"
      via: "getMenuForUnit + getUnitBySlug"
      pattern: "getMenuForUnit|getUnitBySlug"
    - from: "src/app/r/[restaurantSlug]/[unitSlug]/product-dialog.tsx"
      to: "src/app/r/[restaurantSlug]/[unitSlug]/cart-provider.tsx"
      via: "useCart().dispatch ADD"
      pattern: "useCart|dispatch"
    - from: "src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx"
      to: "src/lib/menu/format.ts"
      via: "formatBRL for line prices + subtotal"
      pattern: "formatBRL"
---

<objective>
Wire the complete menu + cart UI for a selected unit: the per-unit layout (CartProvider), the menu Server Component, the Tabs/Destaques menu view with sticky unit indicator, the product Dialog, the floating cart button, and the bottom-sheet cart with inline editing.

Purpose: Delivers MENU-02/03/04/05 (browsing) and CART-01/02 (add/adjust/remove), implementing D-05/D-06/D-07/D-08/D-09/D-11. Depends on Plan 01 (queries/format/tabs/sheet) and Plan 03 (CartProvider/useCart/CartItem).
Output: layout.tsx, page.tsx, menu-view.tsx, product-dialog.tsx, cart-fab.tsx, cart-sheet.tsx under the [unitSlug] segment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-public-customer-menu-selection-browsing-cart/05-RESEARCH.md
@.planning/phases/05-public-customer-menu-selection-browsing-cart/05-01-SUMMARY.md
@.planning/phases/05-public-customer-menu-selection-browsing-cart/05-03-SUMMARY.md

<interfaces>
<!-- From Plan 01 (queries.ts, format.ts) and Plan 03 (cart-provider.tsx, cart-types.ts). -->
  getUnitBySlug(restaurantId, unitSlug): Promise<Unit | null>
  getRestaurantBySlug(slug): Promise<Restaurant | null>   // filters isActive
  getMenuForUnit(restaurantId, unitId): Promise<{ categories: CategoryWithProducts[]; featured: Product[] }>
    CategoryWithProducts = { id, name, sortOrder, products: Product[] }
    Product = { id, name, description: string|null, price: string (NUMERIC->STRING), imageUrl: string|null, isFeatured: boolean, sortOrder, categoryId, ... }
  formatBRL(price: number): string                          // price MUST be Number(product.price)

  // Plan 03 cart contract (confirm in 05-03-SUMMARY.md):
  CartProvider({ children, storageKey })   // storageKey = cart:${restaurantId}:${unitId}
  useCart(): { state: { items: CartItem[] }, dispatch }
  CartItem = { productId, name, price: number, qty, notes }
  Actions: ADD {item}, SET_QTY {productId, qty}, REMOVE {productId}, HYDRATE {items}
    (ADD merges by productId; SET_QTY with qty<=0 removes)
  import type { CartItem } from '@/lib/menu/cart-types'

  Base UI shadcn components available (Plan 01): src/components/ui/tabs.tsx, src/components/ui/sheet.tsx, plus existing dialog.tsx, button.tsx, card.tsx, badge.tsx, textarea.tsx.
  CRITICAL: read the generated tabs.tsx and sheet.tsx for their exact exported names/props (Base UI API differs from Radix — e.g. onValueChange may pass string|null). Use the components as exported, do not assume Radix prop names.

  next/image: remotePatterns for *.supabase.co already configured. Use <Image> for product photos (imageUrl). Guard null imageUrl with a placeholder div.
  Next.js 16: params is a Promise — await it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit layout (CartProvider) + menu page Server Component</name>
  <files>src/app/r/[restaurantSlug]/[unitSlug]/layout.tsx, src/app/r/[restaurantSlug]/[unitSlug]/page.tsx</files>
  <read_first>
    - .planning/phases/05-public-customer-menu-selection-browsing-cart/05-01-SUMMARY.md and 05-03-SUMMARY.md (exact query + cart signatures)
    - src/app/r/[restaurantSlug]/[unitSlug]/cart-provider.tsx (CartProvider props)
    - src/lib/menu/queries.ts (getRestaurantBySlug, getUnitBySlug, getMenuForUnit)
    - src/app/painel/disponibilidade/page.tsx (Server Component conventions)
  </read_first>
  <action>
    PART A — src/app/r/[restaurantSlug]/[unitSlug]/layout.tsx (async Server Component). Resolve restaurant + unit to IDs and wrap children in CartProvider with the D-10 storage key. Call notFound() if either is missing (D-12 catches bad unit slugs here so a cart never mounts for an invalid unit):
    - import { notFound } from 'next/navigation'
    - import { getRestaurantBySlug, getUnitBySlug } from '@/lib/menu/queries'
    - import { CartProvider } from './cart-provider'
    - export default async function UnitLayout({ children, params }: { children: React.ReactNode; params: Promise<{ restaurantSlug: string; unitSlug: string }> })
    - const { restaurantSlug, unitSlug } = await params
    - const restaurant = await getRestaurantBySlug(restaurantSlug); if (!restaurant) notFound()
    - const unit = await getUnitBySlug(restaurant.id, unitSlug); if (!unit) notFound()
    - return <CartProvider storageKey={`cart:${restaurant.id}:${unit.id}`}>{children}</CartProvider>

    PART B — src/app/r/[restaurantSlug]/[unitSlug]/page.tsx (async Server Component). Re-resolve and fetch the menu, render MenuView + CartFab:
    - import { notFound } from 'next/navigation'
    - import { getRestaurantBySlug, getUnitBySlug, getMenuForUnit } from '@/lib/menu/queries'
    - import MenuView from './menu-view'; import CartFab from './cart-fab'
    - export default async function MenuPage({ params }: { params: Promise<{ restaurantSlug: string; unitSlug: string }> })
    - const { restaurantSlug, unitSlug } = await params
    - const restaurant = await getRestaurantBySlug(restaurantSlug); if (!restaurant) notFound()
    - const unit = await getUnitBySlug(restaurant.id, unitSlug); if (!unit) notFound()
    - const { categories, featured } = await getMenuForUnit(restaurant.id, unit.id)
    - return ( <> <MenuView unitName={unit.name} categories={categories} featured={featured} /> <CartFab /> </> )

    NO `export const dynamic` (Next.js 16 dynamic-by-default; force-dynamic is a forbidden no-op per RESEARCH). Pass plain serializable props (the Drizzle rows are plain objects); categories/featured pass straight through to the client MenuView.
  </action>
  <verify>
    <automated>test -f "src/app/r/[restaurantSlug]/[unitSlug]/layout.tsx" && test -f "src/app/r/[restaurantSlug]/[unitSlug]/page.tsx" && grep -q "CartProvider" "src/app/r/[restaurantSlug]/[unitSlug]/layout.tsx" && grep -q "getMenuForUnit" "src/app/r/[restaurantSlug]/[unitSlug]/page.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - layout.tsx exists, awaits params, calls notFound() on missing restaurant AND missing unit, and wraps children in CartProvider with storageKey of the exact form `cart:${restaurant.id}:${unit.id}` (grep: `cart:` and `CartProvider`)
    - page.tsx exists, awaits params, calls getMenuForUnit, renders MenuView and CartFab
    - Neither file contains `export const dynamic`
  </acceptance_criteria>
  <done>The unit layout mounts a per-unit CartProvider and 404s invalid units; the page fetches the availability-filtered menu and renders the view + cart button.</done>
</task>

<task type="auto">
  <name>Task 2: MenuView (Tabs + Destaques + sticky indicator) + ProductDialog</name>
  <files>src/app/r/[restaurantSlug]/[unitSlug]/menu-view.tsx, src/app/r/[restaurantSlug]/[unitSlug]/product-dialog.tsx</files>
  <read_first>
    - src/components/ui/tabs.tsx (Base UI Tabs — exact exported component names + props; do NOT assume Radix API)
    - src/components/ui/dialog.tsx (existing Dialog exports: Dialog, DialogTrigger, DialogContent, DialogTitle, DialogDescription, DialogFooter, DialogClose)
    - src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/ui/button.tsx, src/components/ui/textarea.tsx
    - src/lib/menu/format.ts (formatBRL)
    - src/app/r/[restaurantSlug]/[unitSlug]/cart-provider.tsx (useCart, dispatch ADD)
    - src/lib/menu/cart-types.ts (CartItem)
  </read_first>
  <action>
    PART A — src/app/r/[restaurantSlug]/[unitSlug]/menu-view.tsx ('use client').
    Props: { unitName: string; categories: Array<{ id: string; name: string; products: ProductRow[] }>; featured: ProductRow[] } where ProductRow = { id; name; description: string|null; price: string; imageUrl: string|null; isFeatured: boolean }.
    Layout (mobile-first, top to bottom):
      1. Sticky header (D-05): className="sticky top-0 z-20 ..." showing "Você está em: {unitName}". Always visible on scroll.
      2. Destaques strip (D-07): if featured.length > 0, a horizontally-scrollable row of featured product cards, rendered ABOVE the tabs and OUTSIDE the Tabs component so it stays visible regardless of selected tab. Each featured card opens the same ProductDialog.
      3. Tabs (D-06): one tab per category, in the order received (already sortOrder asc from the query). Default selected = first category. One category's product grid visible at a time. Use the Base UI Tabs components exactly as exported from tabs.tsx (read that file first; the trigger list is the tab bar, each panel holds that category's product grid). Make the tab list horizontally scrollable for many categories.
      4. Each product card (Destaques and category panels): photo via next/image <Image src={imageUrl} .../> with a placeholder div when imageUrl is null; name; truncated description; price via formatBRL(Number(product.price)); a Badge "Destaque" for isFeatured products. The whole card is the trigger that opens ProductDialog for that product.
    Use a single controlled dialog driven by a selected-product state (lighter than one dialog per product): const [selected, setSelected] = useState<ProductRow | null>(null); card onClick sets it; render one <ProductDialog product={selected} open={!!selected} onOpenChange={(o)=>!o && setSelected(null)} />.
    Defensive empty: if categories.length === 0, render a "Cardápio em breve" message (the query already hides empty categories but the entire menu may be empty).

    PART B — src/app/r/[restaurantSlug]/[unitSlug]/product-dialog.tsx ('use client', D-09/CART-01).
    Props: { product: ProductRow | null; open: boolean; onOpenChange: (open: boolean) => void }.
    Inside DialogContent (only when product is non-null):
      - Large product photo (next/image, placeholder when imageUrl null), name as DialogTitle, description as DialogDescription.
      - Quantity stepper: state qty (default 1), − and + Buttons with the number between; minimum 1 (the − button does not go below 1).
      - Notes: <Textarea> bound to state notes (default ''), placeholder "Alguma observação? (ex: sem cebola)".
      - DialogFooter Button "Adicionar ao carrinho": on click dispatch({ type: 'ADD', item: { productId: product.id, name: product.name, price: Number(product.price), qty, notes } }) then onOpenChange(false) and reset qty=1/notes=''. Import useCart from './cart-provider'.
      - Reset qty/notes whenever a new product opens (useEffect on product?.id).
    CRITICAL: Number(product.price) — products.price is a STRING from Drizzle; CartItem.price is a number. Never pass the raw string into the cart.
  </action>
  <verify>
    <automated>grep -q "use client" "src/app/r/[restaurantSlug]/[unitSlug]/menu-view.tsx" && grep -q "sticky" "src/app/r/[restaurantSlug]/[unitSlug]/menu-view.tsx" && grep -q "Number(product.price)" "src/app/r/[restaurantSlug]/[unitSlug]/product-dialog.tsx" && grep -q "ADD" "src/app/r/[restaurantSlug]/[unitSlug]/product-dialog.tsx" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - menu-view.tsx starts with 'use client', imports formatBRL from @/lib/menu/format, imports the Tabs components from @/components/ui/tabs
    - menu-view.tsx contains `sticky top-0` (D-05 indicator) and renders the Destaques strip outside the Tabs component (featured cards present when featured.length > 0)
    - menu-view.tsx formats prices with formatBRL(Number( (grep: `formatBRL(Number(`)
    - product-dialog.tsx starts with 'use client', imports useCart, dispatches `type: 'ADD'`, and converts price with `Number(product.price)`
    - product-dialog.tsx contains a Textarea (notes) and a qty stepper with − and + controls
  </acceptance_criteria>
  <done>Menu renders sticky unit indicator, always-visible Destaques strip, category Tabs with availability-filtered products and pt-BR prices; tapping a product opens a Dialog that adds to the cart with qty + notes.</done>
</task>

<task type="auto">
  <name>Task 3: CartFab (floating button) + CartSheet (bottom sheet, inline edit)</name>
  <files>src/app/r/[restaurantSlug]/[unitSlug]/cart-fab.tsx, src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx</files>
  <read_first>
    - src/components/ui/sheet.tsx (Base UI Sheet — exact exports + how to set side="bottom"; read this file, Base UI API differs from Radix)
    - src/components/ui/button.tsx, src/components/ui/separator.tsx
    - src/lib/menu/format.ts (formatBRL)
    - src/app/r/[restaurantSlug]/[unitSlug]/cart-provider.tsx (useCart, dispatch SET_QTY/REMOVE)
    - .planning/phases/05-public-customer-menu-selection-browsing-cart/05-03-SUMMARY.md (cart action semantics: SET_QTY<=0 removes)
  </read_first>
  <action>
    PART A — src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx ('use client', D-08/D-11/CART-02).
    Props: { open: boolean; onOpenChange: (open: boolean) => void }.
    Use the Sheet component with side="bottom" (a bottom sheet). Read sheet.tsx for the exact Sheet/SheetContent/SheetHeader/SheetTitle export names and the prop that sets the side.
    Content (from useCart()):
      - Title "Seu carrinho".
      - If state.items.length === 0: a "Seu carrinho está vazio" empty message.
      - Else, list each item: name, qty, line price formatBRL(item.price * item.qty), the item.notes (muted, if present), an inline stepper (− and + Buttons) and a remove Button (trash icon or "Remover").
        * − Button: dispatch({ type: 'SET_QTY', productId: item.productId, qty: item.qty - 1 }) (reaching 0 removes the item per Plan 03 reducer).
        * + Button: dispatch({ type: 'SET_QTY', productId: item.productId, qty: item.qty + 1 }).
        * Remove Button: dispatch({ type: 'REMOVE', productId: item.productId }).
      - Subtotal row: formatBRL(state.items.reduce((s, i) => s + i.price * i.qty, 0)).
    No "send order" / WhatsApp button here — that is Phase 6, explicitly out of scope.

    PART B — src/app/r/[restaurantSlug]/[unitSlug]/cart-fab.tsx ('use client', D-08).
    - const { state } = useCart(); const count = state.items.reduce((s, i) => s + i.qty, 0)
    - const [open, setOpen] = useState(false)
    - Render a fixed floating Button: className="fixed bottom-4 left-1/2 -translate-x-1/2 z-30 ..." labeled `Ver carrinho — ${count} ${count === 1 ? 'item' : 'itens'}`.
    - Hide the FAB entirely when count === 0 (return null) so an empty cart shows no button.
    - Clicking it setOpen(true); render <CartSheet open={open} onOpenChange={setOpen} />.
    Hydration note: because the cart hydrates after mount (Plan 03), count is 0 on first paint then updates — that is expected and fine; do not read localStorage here.
  </action>
  <verify>
    <automated>grep -q "use client" "src/app/r/[restaurantSlug]/[unitSlug]/cart-fab.tsx" && grep -q "Ver carrinho" "src/app/r/[restaurantSlug]/[unitSlug]/cart-fab.tsx" && grep -q "SET_QTY" "src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx" && grep -q "REMOVE" "src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx" && grep -q "formatBRL" "src/app/r/[restaurantSlug]/[unitSlug]/cart-sheet.tsx" && echo OK && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - cart-fab.tsx starts with 'use client', uses useCart to compute a count, renders a `fixed` button labeled with "Ver carrinho —" and the count, returns null when count === 0
    - cart-sheet.tsx starts with 'use client', uses the Sheet component with bottom side, dispatches `SET_QTY` (for + and −) and `REMOVE`, and shows a subtotal via formatBRL
    - cart-sheet.tsx contains NO wa.me / WhatsApp / "Enviar pedido" send logic (Phase 6 scope)
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>A floating cart button shows the live item count and opens a bottom sheet where each line can be incremented/decremented/removed inline, with a running subtotal — no send action (Phase 6).</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (whole app compiles).
- `npm run verify-menu` still green (server-side data unaffected).
- `npm run dev` manual click-through (phase gate, no harness in this project): pick unit → menu with tabs + Destaques + sticky unit name → tap product → set qty + notes → add → FAB shows count → open sheet → +/−/remove updates subtotal → reload page → cart persists → switch units → cart is isolated.
</verification>

<success_criteria>
- MENU-02/03/04/05 satisfied (category tabs in order, availability-filtered, featured strip, pt-BR prices, mobile-first).
- CART-01/02 satisfied (add with qty+notes via Dialog; adjust/remove inline in the sheet).
- D-05/D-06/D-07/D-08/D-09/D-11/D-12 implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/05-public-customer-menu-selection-browsing-cart/05-04-SUMMARY.md`. Document the final route structure, confirm `npx tsc --noEmit` and `npm run verify-menu` are green, and write the exact manual cart click-through steps (add → adjust → remove → reload persists → switch-unit isolation) to be executed during /gsd:verify-work since there is no client-side test harness.
</output>
