---
phase: 02-platform-super-admin-restaurant-provisioning
plan: 04
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/admin/(dashboard)/page.tsx
  - src/app/admin/(dashboard)/restaurant-table.tsx
  - src/app/admin/(dashboard)/restaurant-form-dialog.tsx
  - src/app/admin/(dashboard)/deactivate-alert-dialog.tsx
  - src/app/admin/(dashboard)/created-success-dialog.tsx
autonomous: false
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, PLAT-05]
must_haves:
  truths:
    - "Super admin sees a table of all restaurants with columns Nome, Link, Status, Criado em, Admins, Ações"
    - "Status shows a green 'ativo' badge or gray 'inativo' badge per is_active"
    - "Clicking 'Novo Restaurante' opens a Dialog with name, slug (auto-filled from name, editable), and admin email fields"
    - "Submitting create calls createRestaurant and, on success, shows the menu link and one-time temp password"
    - "Editing a restaurant opens the same Dialog in edit mode; changing the slug of an active restaurant shows the D-06 warning"
    - "'Desativar' opens an AlertDialog confirmation before deactivating; 'Ativar' is a direct action"
    - "Empty state shows 'Nenhum restaurante cadastrado' copy"
  artifacts:
    - path: "src/app/admin/(dashboard)/page.tsx"
      provides: "restaurants listing with admin count, renders restaurant-table"
      contains: "RestaurantTable"
    - path: "src/app/admin/(dashboard)/restaurant-table.tsx"
      provides: "client Table with row actions (Editar, Ativar/Desativar)"
      contains: "Table"
    - path: "src/app/admin/(dashboard)/restaurant-form-dialog.tsx"
      provides: "create/edit Dialog form calling createRestaurant/updateRestaurant"
      contains: "Dialog"
    - path: "src/app/admin/(dashboard)/deactivate-alert-dialog.tsx"
      provides: "AlertDialog deactivate confirmation"
      contains: "AlertDialog"
    - path: "src/app/admin/(dashboard)/created-success-dialog.tsx"
      provides: "one-time temp-password + link display"
      contains: "Senha temporária"
  key_links:
    - from: "src/app/admin/(dashboard)/restaurant-form-dialog.tsx"
      to: "createRestaurant / updateRestaurant"
      via: "import from @/lib/restaurants/actions"
      pattern: "createRestaurant|updateRestaurant"
    - from: "src/app/admin/(dashboard)/deactivate-alert-dialog.tsx"
      to: "toggleRestaurantActive"
      via: "import from @/lib/restaurants/actions"
      pattern: "toggleRestaurantActive"
    - from: "src/app/admin/(dashboard)/page.tsx"
      to: "restaurants + admin_users count"
      via: "@supabase/ssr RLS-scoped query (preserves Phase 1 security proof)"
      pattern: "from\\('restaurants'\\)"
---

<objective>
Build the super-admin restaurant management UI: evolve the dashboard from a Card list into a shadcn Table (PLAT-04, D-01), add the create/edit Dialog with auto-slug and admin-email provisioning (PLAT-01/02/05, D-03/04/06), the deactivate AlertDialog (PLAT-03, D-12), and the one-time success dialog showing the menu link + temp password (D-08). Ends with a human-verify checkpoint for the UI-only behaviors (D-06 warning, D-12 confirmation) that the tsx scripts cannot assert.

Purpose: This is the user-facing surface that makes PLAT-01..05 usable. It wires the Plan 02 Server Actions to the shadcn components from Plan 01.
Output: One evolved page + four new client components, plus a passed human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-platform-super-admin-restaurant-provisioning/02-CONTEXT.md
@.planning/phases/02-platform-super-admin-restaurant-provisioning/02-UI-SPEC.md
@.planning/phases/02-platform-super-admin-restaurant-provisioning/02-RESEARCH.md
@.planning/phases/02-platform-super-admin-restaurant-provisioning/02-01-SUMMARY.md
@.planning/phases/02-platform-super-admin-restaurant-provisioning/02-02-SUMMARY.md

<interfaces>
<!-- Contracts to consume. Use directly. -->

From src/lib/restaurants/actions.ts (Plan 02):
```typescript
createRestaurant(input: { name: string; slug: string; adminEmail: string })
  : Promise<{ success: true; restaurant: { id; name; slug }; tempPassword: string; adminEmail: string }
          | { error: Record<string, string[]> }>
updateRestaurant(input: { id: string; name: string; slug: string })
  : Promise<{ success: true } | { error: Record<string, string[]> }>
toggleRestaurantActive(id: string, isActive: boolean): Promise<{ success: true }>
```

From src/lib/restaurants/slug.ts (Plan 01):
```typescript
export function generateSlug(name: string): string
```

From src/lib/restaurants/schema.ts (Plan 01): createRestaurantSchema, updateRestaurantSchema (for react-hook-form zodResolver).

From src/app/admin/(dashboard)/page.tsx (current — RLS-scoped @supabase/ssr query; KEEP this client for the listing, do NOT switch to Drizzle — RESEARCH Pattern 4 "Important caveat"):
```typescript
const supabase = await createClient()
const { data: restaurants } = await supabase
  .from('restaurants').select('id, name, slug, is_active, created_at').order('name')
```

Existing shadcn components available: button, card, form, input, label, table, dialog, badge, alert-dialog (Plan 01 added the last four).
</interfaces>

<ui_spec_contract>
Authoritative copy/visuals from 02-UI-SPEC.md — use VERBATIM:
- Table headers (D-01): "Nome" / "Link" / "Status" / "Criado em" / "Admins" / "Ações"
- Status badge: ativo → Badge with `bg-green-100 text-green-800 dark:bg-green-900/30 dark:text-green-400`, label "ativo"; inativo → `Badge variant="secondary"`, label "inativo". (The green badge is the ONE approved exception to the neutral palette — no other green anywhere.)
- Primary CTA open create: "Novo Restaurante"; submit create: "Criar Restaurante"; submit edit: "Salvar Alterações"
- Row actions: "Editar"; "Ativar" (direct, no dialog); "Desativar" (opens AlertDialog)
- Empty heading: "Nenhum restaurante cadastrado"
- Empty body: "Cadastre o primeiro restaurante da plataforma para começar. Clique em \"Novo Restaurante\" para criar o cadastro e provisionar o admin."
- Slug collision error (D-05): "Este link já está em uso. Escolha outro slug para o restaurante."
- Slug edit warning (D-06, non-blocking, only when editing an ACTIVE restaurant and slug changed): "Atenção: ao alterar o link, o endereço antigo (/r/{slug-antigo}) deixará de funcionar."
- Deactivate AlertDialog (D-12): Title "Desativar restaurante?"; Body "O admin deste restaurante não conseguirá mais fazer login enquanto estiver desativado. Você pode reativar quando quiser."; Confirm "Desativar"; Cancel "Cancelar"
- Success dialog (D-07/D-08): Heading "Restaurante criado com sucesso"; Body "Compartilhe o link e a senha temporária abaixo com o administrador do restaurante. A senha não será exibida novamente."; password label "Senha temporária (copie agora — não será mostrada novamente)"; link label "Link do cardápio: /r/{slug}"
- Field labels: "Nome do restaurante" / "Link (slug)" / "E-mail do administrador do restaurante"
- Slug helper (D-04): "Gerado automaticamente a partir do nome. Você pode editar antes de salvar."
- Page heading (existing): "Painel da Plataforma -- Restaurantes" (h1, text-xl font-semibold)
- Spacing: page padding p-6, gap-4 between sections, gap-2 form field gaps, Dialog padding 24px (shadcn default), row min-height 44px.
- Color: primary CTA uses --primary (accent, one per screen/dialog); destructive ("Desativar" confirm) uses --destructive. Body/label text 14px (text-sm); titles text-xl/semibold.
</ui_spec_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Evolve dashboard page to a Table with admin count + empty state</name>
  <read_first>
    - src/app/admin/(dashboard)/page.tsx (current Card list — the @supabase/ssr RLS-scoped query to preserve)
    - src/components/ui/table.tsx, src/components/ui/badge.tsx (Plan 01 — component APIs)
    - src/app/admin/login/page.tsx (existing pt-BR tone + className conventions)
    - .planning/phases/02-platform-super-admin-restaurant-provisioning/02-RESEARCH.md ("Pattern 4" — admin-count via second RLS-scoped query, NOT Drizzle)
    - 02-UI-SPEC.md (ui_spec_contract above — verbatim copy + green-badge exception)
  </read_first>
  <action>
**Rewrite `src/app/admin/(dashboard)/page.tsx`** (Server Component). Keep the existing RLS-scoped `@supabase/ssr` `createClient()` for the primary `restaurants` query (preserves the Phase 1 D-09 security proof — do NOT use Drizzle `db` here).

1. Query restaurants: `supabase.from('restaurants').select('id, name, slug, is_active, created_at').order('name')`.
2. Admin count (RESEARCH Pattern 4): run a SECOND RLS-scoped query `supabase.from('admin_users').select('restaurant_id')` (super_admin RLS reads all rows), then count per restaurant_id in JS into a `Map<string, number>`.
3. Keep the h1 "Painel da Plataforma -- Restaurantes" (text-xl font-semibold), page wrapper with gap-4.
4. Render a "Novo Restaurante" trigger (the create Dialog from Task 2 — render `<RestaurantFormDialog mode="create" />` which contains its own trigger Button).
5. If zero restaurants: render the empty state (heading "Nenhum restaurante cadastrado" + the exact body copy from ui_spec_contract) instead of the table.
6. Otherwise render `<RestaurantTable restaurants={rows} adminCounts={...} />` (client component from Task 2), passing serializable plain objects (id, name, slug, is_active, created_at as ISO string, adminCount).

**Create `src/app/admin/(dashboard)/restaurant-table.tsx`** (`'use client'`):
- Renders shadcn `Table` with header row: "Nome" / "Link" / "Status" / "Criado em" / "Admins" / "Ações" (verbatim).
- Each row: name; `/{slug}` (Link column, render as `/r/{slug}` text); Status badge (ativo green / inativo secondary per ui_spec_contract); created_at formatted as pt-BR date (`new Date(...).toLocaleDateString('pt-BR')`); adminCount; Ações cell containing `<RestaurantFormDialog mode="edit" restaurant={row} />` (Editar trigger) plus an Ativar/Desativar control: if `is_active` render `<DeactivateAlertDialog id slug />` (Desativar trigger + AlertDialog); if not active render a direct "Ativar" Button that calls `toggleRestaurantActive(id, true)` then `router.refresh()`.
- Row min-height 44px (`className` on TableCell or row), action buttons use `variant="ghost"` or `variant="outline"` size sm; the primary CTA color (--primary) is reserved for the page-level "Novo Restaurante" and Dialog submit, not row actions.
- Import the dialog components from the sibling files.
  </action>
  <verify>
    <automated>grep -q "RestaurantTable" src/app/admin/(dashboard)/page.tsx && grep -q "Nenhum restaurante cadastrado" src/app/admin/(dashboard)/page.tsx && grep -q "from('admin_users')" src/app/admin/(dashboard)/page.tsx && grep -q "Criado em" src/app/admin/(dashboard)/restaurant-table.tsx && npx tsc --noEmit 2>&1 | grep -E "dashboard\)/page|restaurant-table" && echo "TYPE ERRORS" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `page.tsx` contains `from('restaurants')` AND `from('admin_users')` (second RLS-scoped count query, no Drizzle `db` import)
    - `page.tsx` does NOT contain `from '@/db'` or `import { db }`
    - `page.tsx` contains `Nenhum restaurante cadastrado` and renders `RestaurantTable`
    - `restaurant-table.tsx` first line is `'use client'`
    - `restaurant-table.tsx` contains all 6 headers: `Nome`, `Link`, `Status`, `Criado em`, `Admins`, `Ações`
    - `restaurant-table.tsx` contains the green-badge class `bg-green-100 text-green-800` and `variant="secondary"`
    - `restaurant-table.tsx` contains `toggleRestaurantActive` (Ativar direct path) and references `DeactivateAlertDialog`
    - `npx tsc --noEmit` reports no errors in these files
  </acceptance_criteria>
  <done>Dashboard renders a Table of all restaurants with status badges, admin counts, pt-BR dates, row actions, and the empty state — using RLS-scoped queries.</done>
</task>

<task type="auto">
  <name>Task 2: Build create/edit form Dialog, deactivate AlertDialog, and success dialog</name>
  <read_first>
    - src/lib/restaurants/actions.ts (createRestaurant/updateRestaurant/toggleRestaurantActive signatures + error shape — Plan 02)
    - src/lib/restaurants/slug.ts and schema.ts (generateSlug, zod schemas — Plan 01)
    - src/components/ui/dialog.tsx, alert-dialog.tsx, form.tsx, input.tsx, label.tsx, button.tsx (component APIs)
    - 02-UI-SPEC.md (ui_spec_contract above — all dialog copy verbatim, D-06 warning condition)
    - .planning/phases/02-platform-super-admin-restaurant-provisioning/02-CONTEXT.md (D-04 editable slug, D-06 warning, D-08 one-time password)
  </read_first>
  <action>
Create three `'use client'` components.

**`src/app/admin/(dashboard)/restaurant-form-dialog.tsx`** — props `{ mode: 'create' | 'edit'; restaurant?: { id; name; slug; is_active } }`:
- Trigger: create mode → primary Button "Novo Restaurante"; edit mode → ghost/outline Button "Editar".
- Use react-hook-form + `@hookform/resolvers/zod` with `createRestaurantSchema` (create) or `updateRestaurantSchema` (edit).
- Fields: "Nome do restaurante" (name); "Link (slug)" (slug) with helper "Gerado automaticamente a partir do nome. Você pode editar antes de salvar."; create mode only: "E-mail do administrador do restaurante" (adminEmail).
- D-04 auto-slug: on name change, if the slug field has NOT been manually edited by the user, set slug = `generateSlug(name)`. Track a `slugTouched` boolean; once the user edits slug directly, stop auto-overwriting.
- D-06 warning (edit mode only): if `restaurant.is_active` is true AND the slug value differs from `restaurant.slug`, render the non-blocking warning text "Atenção: ao alterar o link, o endereço antigo (/r/{restaurant.slug}) deixará de funcionar." (substitute the actual old slug).
- Submit: call `createRestaurant({ name, slug, adminEmail })` (create) or `updateRestaurant({ id, name, slug })` (edit). On `{ error }`, map field errors back onto the form (e.g., `error.slug` → set slug field error using the returned message, which includes the D-05 "Este link já está em uso..." text). On create `{ success }`: close this dialog and open `<CreatedSuccessDialog>` with `restaurant.slug`, `tempPassword`, `adminEmail`. On edit `{ success }`: close dialog and `router.refresh()`.
- Submit button: create → "Criar Restaurante"; edit → "Salvar Alterações" (primary/--primary color).

**`src/app/admin/(dashboard)/deactivate-alert-dialog.tsx`** — props `{ id: string; slug: string }`:
- Trigger Button "Desativar" (variant outline or ghost).
- shadcn `AlertDialog` with Title "Desativar restaurante?", Body "O admin deste restaurante não conseguirá mais fazer login enquanto estiver desativado. Você pode reativar quando quiser.", Cancel "Cancelar", Action "Desativar" (destructive color, `--destructive`).
- On confirm: `await toggleRestaurantActive(id, false)` then `router.refresh()`.

**`src/app/admin/(dashboard)/created-success-dialog.tsx`** — props `{ slug: string; tempPassword: string; adminEmail: string; open; onOpenChange }`:
- shadcn `Dialog` with Heading "Restaurante criado com sucesso", Body "Compartilhe o link e a senha temporária abaixo com o administrador do restaurante. A senha não será exibida novamente.".
- Show "Link do cardápio: /r/{slug}" (read-only) and the password under label "Senha temporária (copie agora — não será mostrada novamente)" with a copy-to-clipboard button (`navigator.clipboard.writeText`).
- Show the adminEmail for reference.
- IMPORTANT (D-08): the tempPassword lives ONLY in this component's props/React state — never write it to localStorage, sessionStorage, a cookie, or any network call. On close, `router.refresh()` so the new restaurant appears in the table.
  </action>
  <verify>
    <automated>grep -q "createRestaurant" src/app/admin/(dashboard)/restaurant-form-dialog.tsx && grep -q "generateSlug" src/app/admin/(dashboard)/restaurant-form-dialog.tsx && grep -q "deixará de funcionar" src/app/admin/(dashboard)/restaurant-form-dialog.tsx && grep -q "Desativar restaurante?" src/app/admin/(dashboard)/deactivate-alert-dialog.tsx && grep -q "Senha temporária" src/app/admin/(dashboard)/created-success-dialog.tsx && npx tsc --noEmit 2>&1 | grep -E "restaurant-form-dialog|deactivate-alert|created-success" && echo "TYPE ERRORS" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `restaurant-form-dialog.tsx` first line `'use client'`; contains `createRestaurant`, `updateRestaurant`, `generateSlug`, and the D-06 string `deixará de funcionar`
    - `restaurant-form-dialog.tsx` contains the three field labels `Nome do restaurante`, `Link (slug)`, `E-mail do administrador do restaurante`
    - `deactivate-alert-dialog.tsx` contains `AlertDialog`, `Desativar restaurante?`, `Cancelar`, and `toggleRestaurantActive`
    - `created-success-dialog.tsx` contains `Restaurante criado com sucesso`, `Senha temporária`, and `navigator.clipboard`
    - `created-success-dialog.tsx` does NOT contain `localStorage`, `sessionStorage`, or `document.cookie` (D-08: temp password never persisted)
    - `npx tsc --noEmit` reports no errors in these three files
  </acceptance_criteria>
  <done>Create/edit Dialog (auto-slug + D-06 warning + provisioning), deactivate AlertDialog (D-12), and one-time success dialog (D-08) implemented and wired to the Plan 02 Server Actions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify UI-only behaviors (D-06 warning, D-12 confirmation, full create flow)</name>
  <what-built>
The full super-admin restaurant management UI: a Table listing (PLAT-04), create/edit Dialog with auto-slug and first-admin provisioning (PLAT-01/02/05), deactivate AlertDialog confirmation (PLAT-03/D-12), slug-edit warning (D-06), and one-time temp-password success dialog (D-08). The tsx scripts (verify-restaurants.ts) already prove the data-layer behaviors; this checkpoint covers the UI-only behaviors (D-06, D-12) and the end-to-end visual flow that scripts cannot assert.
  </what-built>
  <how-to-verify>
Run `npm run dev`, log in at http://localhost:3000/admin/login as the super admin (super@boamidia.dev / changeme123!), then at http://localhost:3000/admin:

1. **PLAT-04 listing:** Confirm a table appears with columns Nome / Link / Status / Criado em / Admins / Ações. Seeded restaurants show an "ativo" green badge.
2. **PLAT-01 + PLAT-05 create:** Click "Novo Restaurante". Type a name (e.g., "Sorveteria Gelato") — confirm the Link (slug) field auto-fills to "sorveteria-gelato". Edit the slug manually and confirm it stops auto-updating when you change the name again. Enter an admin email. Click "Criar Restaurante".
3. **D-08 success:** Confirm the success dialog shows "Link do cardápio: /r/sorveteria-gelato" and a temp password under "Senha temporária (copie agora — não será mostrada novamente)" with a working copy button. Close it; confirm the new restaurant now appears in the table with Admins = 1.
4. **D-05 collision:** Open "Novo Restaurante" again, reuse the same slug, submit — confirm the inline error "Este link já está em uso. Escolha outro slug para o restaurante." appears and no row is created.
5. **PLAT-02 + D-06 warning:** Click "Editar" on an ACTIVE restaurant, change the slug — confirm the warning "Atenção: ao alterar o link, o endereço antigo (/r/...) deixará de funcionar." appears. Save with "Salvar Alterações" and confirm the change persists.
6. **PLAT-03 + D-12:** Click "Desativar" on a restaurant — confirm an AlertDialog titled "Desativar restaurante?" appears. Click "Cancelar" → status stays ativo. Click "Desativar" again then confirm → status flips to a gray "inativo" badge. Click "Ativar" (direct, no dialog) → status returns to ativo.
7. (Optional) Log out, attempt to log in as the newly-provisioned restaurant admin using the temp password you copied → should reach /painel. Deactivate that restaurant, attempt login again → should be denied with "Este restaurante está desativado..." (D-11, from Plan 03).
  </how-to-verify>
  <resume-signal>Type "approved" if all behaviors work, or describe any issues (which step, what happened vs expected).</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` reports no errors across the five files.
- `npx tsx scripts/verify-restaurants.ts` still exits 0 (data layer regression check).
- Human-verify checkpoint approved for D-06, D-12, and the end-to-end create/provision/deactivate flow.
</verification>

<success_criteria>
- Table listing with status badges, admin counts, row actions, and empty state (PLAT-04, D-01).
- Create/edit Dialog with auto-slug, D-05 inline error, D-06 warning, and admin provisioning (PLAT-01/02/05).
- Deactivate AlertDialog confirmation + direct Ativar (PLAT-03, D-12).
- One-time temp-password success dialog, never persisted (D-08).
- Human checkpoint passed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-platform-super-admin-restaurant-provisioning/02-04-SUMMARY.md`
</output>
