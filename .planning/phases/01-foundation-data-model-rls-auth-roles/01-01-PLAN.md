---
phase: 01-foundation-data-model-rls-auth-roles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - .env.example
  - .env
  - .gitignore
  - drizzle.config.ts
  - src/db/index.ts
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: supabase
    why: "Project provides Postgres database, Auth (admin login), and is the foundation for every later phase. No Supabase project exists yet for this greenfield repo."
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Data API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API Keys -> publishable key (sb_publishable_...)"
      - name: SUPABASE_SECRET_KEY
        source: "Supabase Dashboard -> Project Settings -> API Keys -> secret key (sb_secret_...) -- server-only, never NEXT_PUBLIC_"
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> Session pooler (port 5432) -- used by drizzle-kit"
      - name: DATABASE_URL_RUNTIME
        source: "Supabase Dashboard -> Project Settings -> Database -> Connection string -> Transaction pooler (port 6543) -- used by app runtime"
    dashboard_config:
      - task: "Create a new Supabase project (any region, set a database password)"
        location: "https://supabase.com/dashboard -> New Project"

must_haves:
  truths:
    - "A Next.js 16 App Router project exists and runs locally"
    - "Drizzle ORM is configured and can connect to the Supabase Postgres instance"
    - "Environment variables for Supabase (URL, anon/publishable key, secret key) and both DB connection strings (session + transaction mode) are documented and present locally"
  artifacts:
    - path: "package.json"
      provides: "Next.js 16 + Supabase + Drizzle + zod + shadcn dependency set"
      contains: "next"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointing at DATABASE_URL (session mode) with entities.roles.provider: 'supabase'"
      contains: "entities"
    - path: "src/db/index.ts"
      provides: "Runtime Drizzle client using DATABASE_URL_RUNTIME (transaction mode)"
      contains: "DATABASE_URL_RUNTIME"
    - path: ".env.example"
      provides: "Documented env var names for Supabase + both DB connection strings"
      contains: "DATABASE_URL_RUNTIME"
  key_links:
    - from: "drizzle.config.ts"
      to: "DATABASE_URL"
      via: "dbCredentials.url reading process.env.DATABASE_URL"
      pattern: "DATABASE_URL"
    - from: "src/db/index.ts"
      to: "DATABASE_URL_RUNTIME"
      via: "postgres.js client constructor reading process.env.DATABASE_URL_RUNTIME"
      pattern: "DATABASE_URL_RUNTIME"
---

<objective>
Scaffold the Next.js 16 + TypeScript + Tailwind 4 project from scratch (this repo is currently empty except `.planning/` and `CLAUDE.md`), install the full Phase 1 dependency set (Supabase, Drizzle, zod, shadcn/ui, dev tooling), provision a Supabase project, and wire up environment variables + Drizzle config for both Session Mode (migrations) and Transaction Mode (runtime).

Purpose: Every later plan in this phase (schema, RLS, auth) depends on a working Next.js app, installed dependencies, a live Supabase project, and correctly-configured `.env` + `drizzle.config.ts` + `src/db/index.ts`. This is the unblocking foundation.
Output: Running Next.js 16 app (`npm run dev` works), all dependencies installed, shadcn/ui initialized with base components, `.env`/`.env.example` populated, `drizzle.config.ts` and `src/db/index.ts` created and able to connect to the live Supabase Postgres instance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-model-rls-auth-roles/01-CONTEXT.md
@.planning/phases/01-foundation-data-model-rls-auth-roles/01-RESEARCH.md

<interfaces>
<!-- This plan creates the foundational files every later plan in Phase 1 depends on. -->
<!-- No prior plans exist (this is plan 01, wave 1) -- nothing to extract from history. -->

Target drizzle.config.ts shape (from 01-RESEARCH.md "Code Examples"):
```typescript
import { defineConfig } from 'drizzle-kit'
import { config } from 'dotenv'

config({ path: '.env' })

export default defineConfig({
  schema: './src/db/schema.ts',
  out: './src/db/migrations',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
  entities: {
    roles: {
      provider: 'supabase',
    },
  },
})
```

Target src/db/index.ts shape (runtime client, Transaction Mode):
```typescript
import { drizzle } from 'drizzle-orm/postgres-js'
import postgres from 'postgres'
import * as schema from './schema'

const connectionString = process.env.DATABASE_URL_RUNTIME!
const client = postgres(connectionString, { prepare: false })
export const db = drizzle(client, { schema })
```
Note: `src/db/schema.ts` does not exist yet -- it is created in plan 01-02. This plan's `src/db/index.ts` will fail to compile until `./schema` exists; that is expected and resolved by plan 01-02 (wave 2, depends on this plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 app and install full dependency set</name>
  <files>package.json, tsconfig.json, next.config.ts, .gitignore, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, components.json</files>
  <read_first>
    - .planning/research/STACK.md (exact versions and installation commands)
    - .planning/phases/01-foundation-data-model-rls-auth-roles/01-RESEARCH.md (Standard Stack -> Installation block)
  </read_first>
  <action>
    The repo root currently contains only `.git/`, `.planning/`, `CLAUDE.md`, `.gitignore` (partial), `.DS_Store`. Run the scaffold and installs from the repo root (target `.` so the existing `.planning/` and `CLAUDE.md` are preserved):

    1. `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm`
       - If prompted about existing files, choose to proceed/merge (do not overwrite `.planning/`, `CLAUDE.md`, `.gitignore`, `.git/`).
       - Accept Next.js 16 defaults (Turbopack is default in 16, keep it).

    2. Install core data/auth dependencies:
       `npm install @supabase/supabase-js@2.108.2 @supabase/ssr@0.12.0 drizzle-orm@0.45.2 postgres`

    3. Install validation/forms:
       `npm install zod@^4 react-hook-form@^7 @hookform/resolvers libphonenumber-js`

    4. Install dev dependencies:
       `npm install -D drizzle-kit@0.31.10 tsx dotenv prettier prettier-plugin-tailwindcss`

    5. Initialize shadcn/ui (Tailwind v4 + React 19 compatible CLI):
       `npx shadcn@latest init` (accept defaults for Tailwind v4 / New York style / neutral base color)
       Then add the base components needed for login + landing pages:
       `npx shadcn@latest add button input label card form`

    6. Verify `.gitignore` includes (add if missing): `.env`, `.env*.local`, `node_modules`, `.next`. The existing root `.gitignore` (currently minimal) must be merged with the one `create-next-app` generates -- do not lose either set of entries.

    7. Confirm `npm run dev` starts successfully (Ctrl+C / background-kill after confirming no startup errors -- do not leave a long-running dev server).
  </action>
  <verify>
    <automated>cat package.json | grep -E '"(next|react|@supabase/ssr|drizzle-orm|drizzle-kit|zod|tailwindcss)"'</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains dependencies: `next`, `react`, `react-dom`, `@supabase/supabase-js`, `@supabase/ssr`, `drizzle-orm`, `postgres`, `zod`, `react-hook-form`, `@hookform/resolvers`, `libphonenumber-js`
    - `package.json` contains devDependencies: `drizzle-kit`, `tsx`, `dotenv`, `typescript`, `tailwindcss`
    - `components.json` exists (shadcn/ui initialized)
    - `src/components/ui/button.tsx`, `src/components/ui/input.tsx`, `src/components/ui/card.tsx`, `src/components/ui/form.tsx`, `src/components/ui/label.tsx` exist
    - `.gitignore` contains `.env`, `node_modules`, `.next`
    - `src/app/layout.tsx` and `src/app/page.tsx` exist
    - `npx tsc --noEmit` does not report missing-module errors for installed packages (schema-related errors from later plans are expected at this point)
  </acceptance_criteria>
  <done>Next.js 16 App Router project scaffolded in repo root, all Phase 1 dependencies installed, shadcn/ui initialized with base components, .gitignore covers env files.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create Supabase project and obtain credentials</name>
  <files>none (dashboard-only setup; credentials are written to .env in Task 3)</files>
  <what-built>Task 1 produced a runnable Next.js app with all dependencies installed, but no Supabase project exists yet -- this is a brand-new (greenfield) repo with zero `.env` files.</what-built>
  <action>
    Claude cannot create a new Supabase project via CLI/API without an existing authenticated Supabase account session (no Supabase CLI is installed per Environment Availability in 01-RESEARCH.md, and project creation requires dashboard-level account auth). The user must perform this step manually:

    1. Go to https://supabase.com/dashboard -> New Project
    2. Create a project (any name, e.g. "cardapio-boamidia"; any region close to Brazil, e.g. South America (Sao Paulo)); set and save a strong database password
    3. Wait for provisioning to complete (~2 minutes)
    4. Collect the following from the dashboard:
       - Project Settings -> Data API -> Project URL -> this is `NEXT_PUBLIC_SUPABASE_URL`
       - Project Settings -> API Keys -> publishable key (`sb_publishable_...`) -> this is `NEXT_PUBLIC_SUPABASE_ANON_KEY`
       - Project Settings -> API Keys -> secret key (`sb_secret_...`) -> this is `SUPABASE_SECRET_KEY`
       - Project Settings -> Database -> Connect -> Session pooler connection string (port 5432) -> this is `DATABASE_URL`
       - Project Settings -> Database -> Connect -> Transaction pooler connection string (port 6543) -> this is `DATABASE_URL_RUNTIME`
    5. Paste all five values back here (or confirm they're ready to be placed in `.env` by Claude in Task 3).
  </action>
  <how-to-verify>
    Confirm a Supabase project exists and the 5 values listed above (Project URL, publishable key, secret key, Session Mode connection string, Transaction Mode connection string) have been collected from the dashboard.
  </how-to-verify>
  <verify>
    <automated>MISSING -- this is a checkpoint:human-action task; nothing to run automatically. Task 3's verify command confirms the resulting .env is complete.</automated>
  </verify>
  <resume-signal>Provide the 5 values (or confirm they are ready), or type "done" once you've shared them so Claude can write `.env`</resume-signal>
  <done>A live Supabase project exists and all 5 credential/connection-string values have been collected and shared for Task 3 to write into .env.</done>
</task>

<task type="auto">
  <name>Task 3: Write .env, .env.example, drizzle.config.ts, and runtime Drizzle client stub</name>
  <files>.env, .env.example, drizzle.config.ts, src/db/index.ts</files>
  <read_first>
    - .planning/phases/01-foundation-data-model-rls-auth-roles/01-RESEARCH.md (Code Examples -> drizzle.config.ts, Pitfall 3, Pitfall 5)
    - .gitignore (confirm .env is ignored before writing secrets)
  </read_first>
  <action>
    Using the 5 values collected in Task 2:

    1. Create `.env` (gitignored) with exactly these keys:
    ```
    NEXT_PUBLIC_SUPABASE_URL=<from Task 2>
    NEXT_PUBLIC_SUPABASE_ANON_KEY=<from Task 2>
    SUPABASE_SECRET_KEY=<from Task 2>
    DATABASE_URL=<session pooler connection string, port 5432, from Task 2>
    DATABASE_URL_RUNTIME=<transaction pooler connection string, port 6543, from Task 2>
    ```

    2. Create `.env.example` (committed) with the same keys but placeholder values and inline comments:
    ```
    # Supabase Data API
    NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
    NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxxxx
    # Server-only -- NEVER prefix with NEXT_PUBLIC_, never expose to the browser
    SUPABASE_SECRET_KEY=sb_secret_xxxxx

    # Session Mode (port 5432) -- used ONLY by drizzle-kit (migrations/generate/push)
    DATABASE_URL=postgresql://postgres.xxxxx:[PASSWORD]@aws-0-xx.pooler.supabase.com:5432/postgres

    # Transaction Mode (port 6543) -- used by the app's runtime Drizzle client (src/db/index.ts)
    DATABASE_URL_RUNTIME=postgresql://postgres.xxxxx:[PASSWORD]@aws-0-xx.pooler.supabase.com:6543/postgres
    ```

    3. Create `drizzle.config.ts` at repo root exactly as shown in the interfaces block above: loads `.env` via `dotenv`'s `config({ path: '.env' })`, `dialect: 'postgresql'`, `schema: './src/db/schema.ts'`, `out: './src/db/migrations'`, `dbCredentials.url = process.env.DATABASE_URL!`, and `entities: { roles: { provider: 'supabase' } }` to prevent drizzle-kit from managing Supabase's built-in roles.

    4. Create `src/db/index.ts` exactly as shown in the interfaces block above: a `postgres.js` client built from `process.env.DATABASE_URL_RUNTIME!` with `{ prepare: false }` (required for PgBouncer transaction-mode compatibility), wrapped in `drizzle(client, { schema })` importing `* as schema from './schema'`. This file will show a TypeScript error referencing `./schema` until plan 01-02 creates `src/db/schema.ts` -- that is expected and must NOT be worked around by creating a placeholder schema file in this plan.

    5. Verify the database connection works using the Session Mode URL by running:
       `npx tsx -e "import postgres from 'postgres'; import { config } from 'dotenv'; config(); const sql = postgres(process.env.DATABASE_URL!); sql\`select 1 as ok\`.then((r) => { console.log(JSON.stringify(r)); process.exit(0); }).catch((e) => { console.error(e); process.exit(1); })"`
       This must print a result containing `ok` and exit 0. If it fails, re-check the Task 2 connection string (most common error: wrong password or wrong port).
  </action>
  <verify>
    <automated>npx tsx -e "import { config } from 'dotenv'; config(); const keys=['NEXT_PUBLIC_SUPABASE_URL','NEXT_PUBLIC_SUPABASE_ANON_KEY','SUPABASE_SECRET_KEY','DATABASE_URL','DATABASE_URL_RUNTIME']; const missing = keys.filter(k => !process.env[k]); if (missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('ALL_PRESENT');"</automated>
  </verify>
  <acceptance_criteria>
    - `.env` exists, is gitignored (`git check-ignore .env` exits 0), and contains all 5 keys: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SECRET_KEY`, `DATABASE_URL`, `DATABASE_URL_RUNTIME`
    - `.env.example` exists, is tracked by git, and contains the same 5 keys with placeholder values (no real secrets)
    - `drizzle.config.ts` contains `entities` and `provider: 'supabase'` and `dialect: 'postgresql'`
    - `src/db/index.ts` contains `DATABASE_URL_RUNTIME` and `prepare: false`
    - The `select 1 as ok` connectivity check via `DATABASE_URL` (Session Mode) succeeds and exits 0
  </acceptance_criteria>
  <done>.env and .env.example populated with real (local) / placeholder (example) Supabase credentials and both DB connection strings; drizzle.config.ts and src/db/index.ts created per the locked Session/Transaction mode split (D-11, Pitfall 3); a live `select 1` query against the Supabase Postgres instance succeeds via DATABASE_URL.</done>
</task>

</tasks>

<verification>
After this plan:
1. `npm run dev` starts the Next.js 16 app without errors (manually confirm, do not leave running).
2. `npx tsx -e "...select 1..."` (Task 3 verify command) succeeds against `DATABASE_URL`.
3. `.env` is gitignored; `.env.example` is committed with placeholder values only.
4. `drizzle.config.ts` and `src/db/index.ts` exist and reference the correct env vars per the Session/Transaction mode split.
</verification>

<success_criteria>
- Next.js 16 + TypeScript + Tailwind 4 app scaffolded at repo root, preserving `.planning/`, `CLAUDE.md`, `.git/`.
- All Phase 1 dependencies (Supabase, Drizzle, zod, react-hook-form, libphonenumber-js, shadcn/ui base components) installed.
- A live Supabase project exists with credentials captured in `.env` (gitignored) and documented in `.env.example`.
- `drizzle.config.ts` (Session Mode, port 5432) and `src/db/index.ts` (Transaction Mode, port 6543) are correctly split per D-11/Pitfall 3.
- Database connectivity verified end-to-end with a live `select 1` query.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-model-rls-auth-roles/01-01-SUMMARY.md`
</output>
